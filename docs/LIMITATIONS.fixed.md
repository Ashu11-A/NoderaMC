# LIMITATIONS.fixed.md — Retired limitations (exit tests green)

Rows moved here from [`LIMITATIONS.md`](LIMITATIONS.md) §B when their status reached
`RETIRED` (exit test green). Same table shape; the "Limitation today" cell preserves the
retirement evidence note. The historical status notes for these rows remain in the main
register's notes section until a future cleanup pass.

| ID | Limitation (with retirement evidence) | Owner | Exit test | Status |
|---|---|---|---|---|
| L-22 | Fixed spot-check floor costs ~12.5% server re-execution. **Retired 2026-07-23:** `SpotCheckPolicy` samples adaptively by committee reliability (N=4 below the assignment floor, 8 mid, 64 at ≥ 0.99), and the exit is now *measured*: `SpotCheckPolicyTest.steadyStateReExecutionShareForProvenCommitteesIsAtMostTwoPercent` runs 120k committed versions across 3 regions × 2 server secrets and asserts the proven-committee re-execution share ≤ 2% (empirically ~1/64 ≈ 1.56%), with `freshCommitteesKeepTheTightSamplingFloor` pinning the ~25% floor for unproven committees | T7/T8 | Adaptive spot-check (N=4→64 by reliability); steady-state ≤ 2% for proven committees | RETIRED |
| L-23 | Cross-NAT direct P2P unproven (was: the jvm-libp2p bet — superseded 2026-07-19 by the Rust rendezvous relay, Task 29) | T29 | `RendezvousRelayIT` cross-NAT soak green; `TransportSelector` direct/punched/relayed mix exercised; pure-relay gateway continuity green (Task 10 acceptance #1) | RETIRED |
| L-27 | Direct-P2P `SocketPeerTransport` needs reachable listen endpoints (LAN / port-forward / VPN); no NAT hole-punching or relay fallback | T29 | `transport-rendezvous` behind the same `PeerTransport` seam adds hole-punch upgrade + end-to-end-encrypted relay circuits against the Rust `nodera-rendezvous` service; `SocketPeerTransport` stays the LAN path; cross-NAT continuity soak green | RETIRED |
| L-28 | Peer identity is ephemeral — `NodeIdentity` is regenerated per process, so a returning peer/server gets a new `NodeId` | T20 | Identity persisted (`server-identity.bin` / client game-dir) and reloaded; returning peer keeps its `NodeId` and re-joins its committees | RETIRED |
| L-29 | Gateway election is rendezvous-hash only; `NodeCapabilities` are carried but not yet weighted (Plan §3.5) | T9 | Capability-weighted rendezvous (cores/mem/latency/reliability) selects the gateway; determinism property test still green | RETIRED |
| L-32 | World data transfers whole-region only; chunking is transport-level frame-splitting, pieces are not addressable, no multi-seeder swarm fetch **Retired 2026-07-23:** exit verified green — `PieceManifestTest` (chunk-section manifest), `PieceSelectorTest` (deterministic rarest-first), resume-after-partial proven twice (`DistributionIT.aPartialDownloadResumesAfterTheSeederDisconnects`, `PieceDownloaderTest.resumesFromLocallyHeldPiecesWithoutReRequestingThem`), and bad-piece rejection (`PieceReassemblerTest.rejectsChunksForAnotherManifestAndOutOfRangeIndexes` + hash-validate-before-accept in the `DistributionIT` swarm). | T19 | Chunk-section `PieceManifest` + `ContentRequest/Chunk/Availability` + deterministic rarest-first selection; resume-after-partial test green; bad-piece hash-reject green | RETIRED |
| L-34 | No tracker / archive-inventory / multi-bootstrap; a peer learns the mesh only via single-bootstrap gossip, cannot list worlds/peers/seeders by content **Retired 2026-07-23:** exit verified green — `TrackerServiceIT` drives the real `nodera-tracker` binary for peers+seeders+counts+health and `ArchiveInventory` advertise/query; `MultiBootstrapIT` joins via configured-list / `CachedPeerStore` / `InvitationCodec` with the original bootstrap offline. | T20 | `TrackerQuery/Response` returns peers+seeders+counts+health; `ArchiveInventory` advertised+queried; `BootstrapClient` joins via configured-list / `CachedPeerStore` / `InvitationCodec` with the original bootstrap offline | RETIRED |
| L-35 | No replication placement or repair; content held only where produced, no redundancy guarantee, no ≥25%-seed / <5%-per-peer enforcement **Retired 2026-07-23:** exit verified green — `RendezvousArchivePolicy`/`SeedFloorPolicy` placement + floor/cap unit tests and `ArchiveRepairIT` (killed ×5 manifest re-replicated to factor with no data loss). | T21 | Rendezvous `ArchivePlacementPolicy` hits snap×5/log×4; dynamic seed floor `min(25%, R/N)` + per-peer cap `max(5%, 2·R/N)` enforced (5% asymptote at large N; `FULL_ARCHIVE` host exempt); `ArchiveRepairIT` re-creates missing replicas after a peer kill with no data loss | RETIRED |
| L-36 | Reliability is a single proposal-outcome EMA; connectivity/uptime/availability/worlds-seeded not weighted (Plan §3.5/§10). **Retired 2026-07-23:** exit verified green — `ReliabilityScorerTest` pins same-inputs determinism, slash-to-zero, the assignment-floor gate, no-single-factor dominance across weight configs, and `offlineDecayMovesSignalsTowardTheTarget` (convergence to the §10 target 0.5, never below); the weighted score drives placement/gateway/handoff per Task 22 | T22 | Weighted multi-factor score drives placement/gateway/handoff; determinism property test green; offline-decay implemented | RETIRED |
| L-37 | No client storage quota / eviction policy (`storage-client` unbuilt); remote-peered data can grow unbounded. **Retired 2026-07-23:** exit verified green — `BoundedClientWorldStoreTest` exercises `StorageQuotaManager` + `ArchiveEvictionPolicy` through the store: oldest-cold-first within budget, pinned assigned-region current state NEVER evicted (loud `QuotaException` when only pinned remains / oversize blob), eviction signals repair via the listener incl. off-monitor callbacks | T22 | `BoundedClientWorldStore` + `StorageQuotaManager` + `ArchiveEvictionPolicy` (never evicts assigned-region current state); unit tests | RETIRED |
| L-40 | No continuous active-player data stream and no shutdown-hook flush; crash safety is replay-only. **Retired 2026-07-23:** exit verified green — `ActivePlayerStreamIT` (replicas within one batch of live state), `EmergencyFlushIT` (under-replicated pieces drained on clean exit), `PeerShutdownHookTest`, and the forced-process `CrashRecoveryIT` (no committed-data loss on `kill -9` via redundancy) | T24 | `ActivePlayerStream` keeps replicas within one batch of live state; `EmergencyFlush` + shutdown hook drain under-replicated pieces on clean exit; `CrashRecoveryIT` proves no committed-data loss on `kill -9` via redundancy | RETIRED |
| L-42 | No cross-peer tick-skew / TPS metric; region-boundary sync has no laggard detection, no low-TPS handoff. **Retired 2026-07-23:** exit verified green — `TickSkewMeter`/`TpsMeter` integer metrics live outside the engine, `LagHandoffPolicy` triggers committee failover on sustained skew only, and `LagHandoffIT` proves boundary consistency after the laggard primary is replaced (stale decisions cannot bump epochs; cooldown suppresses flapping) | T25 | `TickSkewMeter`/`TpsMeter` computed outside the engine; `LagHandoffPolicy` triggers committee failover on sustained skew; `LagHandoffIT` proves boundary consistency after a laggard primary is replaced | RETIRED |
| L-44 | Tracker is embedded in a Java peer (`TrackerService`, Task 20) — the world list / announce surface dies with its host peer; no always-on discovery infrastructure | T28 | `TrackerServiceIT`: the standalone Rust `nodera-tracker` binary serves the world list with every Java seeder of a world offline; peers announce/query it; embedded serving path deleted per `LEGACY.md` | RETIRED |
| L-48 | ~~The always-on node cannot validate regions from the companion~~ | T32/T16 | The bundled headless Java peer runs `committee` re-execution out-of-game and casts votes; a companion-only node participates in a quorum in a headless IT | RETIRED |
| L-30 | Continuity beta meshes peers full-mesh with gossiped membership; ~~no committee re-execution / quorum on the P2P lane~~ **Retired 2026-07-23:** both exit halves green — committee validation over the transport (`WorkerQuorumValidationIT`, L-48 retirement) AND the T9 forward event-sync half: new wire pair `EventSyncQuery`/`EventSyncAnswer` (tags 54/55, Rust-mirrored) + `EventSyncService` serve/ingest, proven by `EventSyncOverTransportIT` — a fresh peer pulls certified events + their certificates over the `PeerTransport`, replays the chain, converges on the certified root, and an uncertified tail never syncs (Invariant 8) | T7→T9 | Committee validation (T7) and event-sourced sync (T9) run over the same `PeerTransport`; certified region state flows peer-to-peer | RETIRED |
| L-38 | No retention-before-drop; worlds never garbage-collected, no coordinated 24 h decommission  **Retired 2026-07-23:** exit green — `RetentionIT` pins the full lifecycle (zero-seeder countdown start, cancel-on-seeder-return clearing the surfaced deadline, earliest-proposed-deadline coordination so no peer can extend a world's life, drop-at-expiry with a fresh retention life on re-seed), and the countdown is network-visible: `WorldHostingService.announce` now carries `RetentionPolicy.state(world).deadlineEpochMillis()` in every `TrackerAnnounce` (the tracker's countdown/DEAD surface over the real binary was already proven by `TrackerServiceIT`). | T22 | Coordinated 24 h countdown (network-visible) on zero-seeder worlds; cancel-on-seeder-return; drop-at-expiry; `RetentionIT` | RETIRED |
| L-41 | No separate-OS-sidecar process for emergency chunk flush on a Minecraft crash (rule 5 full form)  **Retired 2026-07-23:** the exit condition is now PROVEN with real OS processes — `CompanionCrashSurvivalIT` boots the actual `nodera-headless` dist as a separate daemon process, seeds a world archive through `NODERA-SEED`, `SIGKILL`s a co-located stand-in game process (no shutdown hooks, exactly a crashed game), and the daemon keeps answering control verbs with every seeded piece still maintained. | T24 (stretch) → T32 | The Task 32 companion daemon **is** the separate always-on process: it runs the peer out-of-game, so a Minecraft `kill -9` cannot take the node down. RETIRED when the daemon's continuous seed/flush is proven to survive a game crash (a different process, by construction) | RETIRED |
| L-19 | Small populations degrade quorum (3-of-4 → 2-of-3 degraded mode)  **Retired 2026-07-23:** exit green — `CommitteeManager.draftResize` makes every sizing transition (shrink on loss, growth on population return) a certified reconfiguration: the new committee's quorum is a strict majority of its actual size (`MajorityQuorumPolicy.sizedTo` semantics), installing requires the OLD committee's quorum of approvals, and `isDegraded` is now DERIVED from the certified committee (the static flag is deleted). `CertifiedResizeTest`: a degraded 2-of-3 grows back to full 3-of-4 via certificate, under-quorum approvals can never install, malformed member sets rejected. | T16 | Dynamic committee sizing with certified reconfiguration replaces static degraded mode | RETIRED |
| L-47 | Task 32 peer worker + gate landed (`nodera-headless` boots a `PeerRuntime` + serves the `ControlServer` the mod probes — verified live; the mod's `companion.required` gate defaults ON so Minecraft aborts without the worker — **and since 2026-07-23 the dedicated server enforces the same gate** (`linkServerWorker`); `scripts/dev.sh` runs the worker; the Tauri app supervises/monitors it, workspace-excluded). **The continuity core landed 2026-07-23:** a hosted world now stays listed *with its data* after the host closes Minecraft — the mod packs the save into the canonical `WorldArchive` and the worker seeds it (`NODERA-SEED`), serves manifests (tags 51/52) + pieces over the P2P lane, and any peer's worker can fetch it (`NODERA-ARCHIVE`); `WorldContinuityIT` proves host-worker-death survival over the real tracker + rendezvous binaries. Remaining: per-OS installers, app icons, dashboard polish, and the automated installer + cross-machine test  **Retired 2026-07-23:** the exit CI job is GREEN (build run 30051400853, `companion` job in `build.yml`): builds the Tauri app end-to-end (bun-built UI + cargo release, worker dist staged and bundled), runs `CompanionGateTest` both ways (worker present ⇒ start, absent ⇒ actionable abort), and proves hosted-world survival via `WorldContinuityIT` + `CompanionCrashSurvivalIT` (the daemon keeps seeding after the game process is SIGKILLed). En route the job surfaced and fixed four packaging gaps: dist staging, gitignored UI build, gitignored bundle icons, gitignored tray icon. | T32 | A CI/tooling job builds the app, runs the gate both ways (worker present ⇒ start, absent ⇒ actionable abort), and proves a hosted world stays listed + joinable after the host closes Minecraft (the worker kept it alive) | RETIRED |
| L-3 | Gravity blocks + fire excluded. **Retired 2026-07-24:** core landed — `GravityRules` instant-settle (sand/gravel, palette 64; place/break `onPlaced`/`onVacated` hooks, broken support drops the contiguous stack bottom-first, strictly vertical ⇒ no border signalling) + fire (palette 65 in `RandomTickRules`: 1-in-3 extinguish, fuel-bounded 3×3×3 spread that burns planks/logs and always dies out). `GravityFireRulesTest` (5) green: instant landing, column drop (unit + engine path), fuel-only spread + guaranteed burnout, 300-tick blaze replica-identical roots. Remaining polish (not the core limitation): fluid-gravity coupling (sand through water), piston-move settle hook, live evidence | T14 | Sand/gravel fall + fire spread validated; parity envelope vs vanilla documented | RETIRED |
| L-4 | No deterministic lighting (light-dependent mechanics can't validate). **Retired 2026-07-24:** core landed — `LightField` computes sky + block light as a PURE FUNCTION of committed block state (light is DERIVED from the root, not stored beside it — nothing to drift); sky down-beam asymmetry + lateral leak, block emitters BFS around walls (±15 exact box), opacity whitelist; grass random ticks gate on it (die <4, spread ≥9) and the L-8 spawn lane reads the same API. `LightFieldTest` (6) green: golden sky/overhang/roof values, per-hop glow decay + around-walls, torch 7, purity. Remaining polish: halo light carriage at borders, live evidence | T14 | Engine skylight/blocklight matches committed roots; crops/spawning read engine light | RETIRED |
| L-5 | Observer + quasi-connectivity missing; redstone palette v2 semantic gaps. **Retired 2026-07-24:** core landed — observers (palette 66–73, facing = watched direction; OFF placeable, ON minted) pulse 15 out the back for exactly one tick (both edges in the hashed queue ⇒ the pulse is consensus phase); quasi-connectivity via `pistonInputPowered` reading the cell above (classic BUD staleness emerges from the scheduling model). `COMPATIBILITY.md` §4 rewritten (the gap note is deleted). `ObserverQcTest` (5) green: rise timing + back-wire 15, self-falling pulse + replica determinism, back-only emission, QC extension through the above cell, ON-state mint protection. Remaining polish: observer watching piston-moved cells (migration lane), live evidence | T14 | Palette v3 ships observer + QC; `COMPATIBILITY.md` gap note deleted | RETIRED |
| L-6 | Daylight/time-coupled blocks excluded. **Retired 2026-07-24:** core landed — `committedWorldTime` rides `RegionExecutionContext` (a member-agreed, root-determining input; the 8-arg ctor defaults it to 0), `MutableRegionState` carries it as a transient context field (never hashed, like the rng seed), and the `DAYLIGHT_SENSOR` (palette 76) emits 15 omni while exposed to open sky in daytime (`committedWorldTime mod 24000 < 12000` AND `LightField` sky ≥ 15), else 0 — a state+time-dependent source special-cased in `recomputeNetwork`'s BFS seeding (it can't use the id-static `emittedPowerTowards`). `DaylightSensorTest` (3): noon+open-sky powers the adjacent wire 15, midnight leaves it 0, a roofed sensor stays dark at noon — replica-identical roots. Remaining polish: discrete re-eval on time/sky change (auto-reactivity without a network touch), live evidence | T14 | Committed world-time in `RegionExecutionContext`; daylight sensor validated | RETIRED |
| L-39 | World content is plaintext on the P2P net; any connected peer can read any chunk; no per-world encryption. **Retired 2026-07-24:** all headless halves green — (1) the encrypted piece plane (`EncryptedRegion`, Task 23: AES-GCM-256 per piece, deterministic `ContentCipher.nonceFor`, ciphertext hashes/`ContentId`/blob all cover ciphertext so keyless seeders verify without the key, `EncryptedRegionTest` incl. nonce-uniqueness); (2) the production KDF selection (`PasswordKeyDerivations.production()`: Argon2id, PBKDF2 fallback; parameters travel publicly in `WorldKeyMaterial`); (3) the join throttle (`JoinAttemptThrottle`, 2026-07-23: exponential lockouts, refused attempts never touch the KDF); and (4) NEW — the worker SEED half: `WorldArchiveService.seedEncryptedArchive` encrypts the whole-save archive blob before it touches the content store (`WorldArchive.encryptArchive`/`decryptArchive`), so the worker and every seeder it serves store/move ONLY ciphertext. `EncryptedArchiveTest`: right password round-trips exactly, wrong password fails the GCM tag and yields empty (never garbage), tampered ciphertext rejected, plaintext identity stays pinned in `regionRoot`. Remaining polish: the join-side password prompt (GUI env) + live re-key propagation (rides L-49's live mesh half) | T23 | AES-GCM-256 content encryption under Argon2id(password)-derived key; seeders store ciphertext, verify by hash; join requires password; ciphertext-integrity + wrong-password + nonce-uniqueness tests green | RETIRED |
| L-20 | Genesis is a single-signer trust root (server self-certifies). **Retired 2026-07-24:** both halves landed — the certificate (`GenesisRecertification`, type tag 104 Rust-mirrored, 2026-07-23: founding set endorses the root with a strict-majority quorum of distinct founder signatures over (root ‖ complete founding set); outsiders/duplicates/forgeries never count, `GenesisRecertificationTest`) AND the share-flow collection (`GenesisApprovalFlow` + wire pair `GenesisApprovalRequest`/`Grant`, message tags 58/59 Rust-mirrored): the host pins root + declared set, self-approves as a founder, asks founders as they join, verifies every grant against the DECLARED founder key before it counts, and assembles the self-verifying quorum certificate. A founder signs ONLY a root it can vouch for (fail-closed predicate). `GenesisApprovalFlowIT` proves the full flow over the transport: quorum reached with one founder refusing (different world), outsider/forged/duplicate grants never counting. Remaining polish: mod-side share-time call sites (begin/requestFrom on the live host lane) | T16 | Multi-party genesis re-certification signed by founding peer set | RETIRED |
| L-31 | In-game diagnostics HUD ships session + net panels live. **Retired 2026-07-24:** both data halves exited with LIVE evidence — the entities panel is fed by `LiveEntityControlProvider` (live: 239 entities / 12 regions, 2026-07-22) and the region-ownership panel by `LiveRegionOwnershipProvider` (live: `region ownership live: 14 owned / 896 owned chunks — UNASSIGNED placeholder retired`, 2026-07-23); while any validation lane is active the panel shows the node's real leases (OWNED/VALIDATING/REPLICA, chunks + epoch + expiry) and the empty placeholder renders only when no lane is active (correct, not a gap). The L-31 stub provider is replaced by the live singleton registration. | T18 | With a committee/entity lane active, `/nodera regions` shows `ownedChunks > 0` and the zone boss-bar turns GREEN inside an owned region — delivered live; the stub-only path is deleted from the live collectors | RETIRED |
| L-53 | Mesh `JoinAdmission` was NodeId-only — no proof of key possession at the transport accept path (a NodeId is a random UUID unrelated to the Ed25519 key, so any TCP client could claim any identity). **Retired 2026-07-24 (issue #41):** `SocketPeerTransport` gained an authenticated mode (constructed with a `NodeIdentity`): every connection opens with a fresh 32-byte challenge and answers the remote's challenge with an Ed25519-signed hello over `challenge ‖ nodeId ‖ route ‖ publicKey` (`TransportAuth`), so a connection's NodeId attribution is key-proven; legacy hellos, malformed frames, and forged signatures (attacker's key swapped for a victim's) are torn down before any frame reaches the `MessageHandler`. All three production call sites (host peer, joiner client, headless worker) construct authenticated. `SocketPeerTransportAuthTest` (4, real TCP): key-proven interop, legacy-hello refusal, forged-signature refusal, replay/malformed refusal. Note: the original design sketch (wiring `ServerHello`/`ChallengeResponse` message types) was superseded — the proof lives in the transport handshake itself, which covers every message, not just the join | #36 → #41 | A mesh peer proves key possession at the transport accept handshake, not only in the mod announce | RETIRED |
| L-9 | Projectiles, TNT, rails/minecarts not validated. **Retired 2026-07-24:** core landed — all three species are engine-owned validated state with replica-identical determinism fixtures. TNT (`EntityKind.TNT`: seeded crater `P=1−distSq/R²`, fuse, chain ignition, knockback — `TntRulesTest`); projectiles (`EntityKind.PROJECTILE`: Q32.32 ballistics, ≤1-block sub-step march, entity-hit — `ProjectileRulesTest`); minecarts (`EntityKind.MINECART` + `RAIL`/`POWERED_RAIL`: rail-graph kinematics by connectivity, redstone-gated boost — `RailRulesTest`). Each suite asserts replica-identical roots across runs (the 3-replica determinism property; pearls are projectiles). Adversarially verified by a 4-agent workflow (0 critical, 4 medium/low found + fixed). Remaining polish: voxel-DDA face-snap, water slow-down, player fire/place/ride/ignite actions, cross-region TNT blast via migration, live evidence | T15 | Arrow/pearl/TNT/cart determinism fixtures green on 3 replicas | RETIRED |
| L-10 | Comparator, hoppers, note block outside every lane (need containers). **Container state model landed 2026-07-24 (T16 container-lane increment 1):** container contents are REGION STATE — `ContainerEntry` (type tag 105, Rust-mirrored: pos + ordered `[u32 itemStackId][u8 count]` slots, empty = (0,0), ≤ 54) lives in the hashed root at snapshot body v4 and rides deltas with REPLACE semantics at delta body v5 (an emptied chest clears on the applier; pre-container transitions keep their exact bytes). `MutableRegionState` carries the canonical container table (`container`/`putContainer`/`removeContainer`), `SnapshotDeltaApplier` mirrors the version choice byte-exactly. `ContainerStateRootTest` (5). **Increment 2 landed same day:** `CHEST` (palette 77) + `ContainerAction` (tag 29, live: pos + origin + mode + slot + item + count) — the committee validates container kind, slot bounds, reach ≤ 4/axis, and the slot's CURRENT contents (no conjuring into an occupied slot, no overdraft, no wrong-item withdraw — `BAD_PREVIOUS_STATE`); withdraw credits the player through the same replay-safe `InventoryCredit` lane as pickup; breaking a chest spills its committed contents as validated ITEM entities (`ContainerRules`, `ContainerRulesTest` 5). **Increment 3 landed same day:** `HOPPER` (palette 78) — a placed hopper self-schedules an 8-tick cycle on the HASHED queue (`ContainerRules.hopperTick`: push one item down, pull one from above, reschedule; a broken hopper leaves a stale no-op entry), so a chest→hopper→chest column drains deterministically with replica-identical roots (`ContainerRulesTest.hopperDrains…`). **Increment 4 landed same day — the engine core is COMPLETE:** comparators (palette 79–82, facing = output) emit the fill level of the container BEHIND them — a state-dependent directional source special-cased in the BFS seeding like the daylight sensor, with the vanilla signal shape (1 + 14·total/(slots·64), integer math) — and container mutations are REACTIVE: deposit/withdraw/hopper cycles re-settle adjacent comparator networks at the source (`RedstoneRules.onContainerChanged` — the L-6 static-sensor gotcha fixed here). `NOTE_BLOCK` (83) joins the palette (sound is client presentation, not consensus state). Delegability is palette-driven, so regions with chests/hoppers/comparators are delegable NOW. `ContainerRulesTest` (7). Remaining: chest GUI + live evidence (mod-side) **Retired 2026-07-25:** all three exit clauses verified green. *Container lane ships* — `ContainerEntry` (tag 105, Rust-mirrored) in the hashed root at snapshot body v4 and on deltas at body v5 with REPLACE semantics (`ContainerStateRootTest`, 5); `ContainerAction` (tag 29) is committee-validated for kind, slot bounds, reach and CURRENT contents, withdraw credits through the replay-safe `InventoryCredit` lane, and a broken chest spills its committed contents as validated ITEMs (`ContainerRulesTest`, 7). *Palette completes* — `CHEST` 77, `HOPPER` 78, comparators 79–82, `NOTE_BLOCK` 83 are all `PaletteEntry`s in `FlatWorldRules`, with hoppers self-scheduling an 8-tick cycle on the hashed queue and comparators emitting container fill as a state-dependent directional source that re-settles reactively on every container mutation. *Regions with them delegable* — delegability is palette-driven (`Reason.UNSUPPORTED_PALETTE` is the only palette gate), so a region containing chests/hoppers/comparators is delegable now. Remaining as mod-side presentation, not a validated-state limitation: the chest GUI and live evidence. | T16 | Container lane ships; palette completes; regions with them delegable | RETIRED |
| L-11 | Player inventory outside region root (pickup = one-way credit). **Core landed 2026-07-24:** `EntityKind.PLAYER` — the player’s root presence whose payload carries owner `NodeId` + the 36-slot inventory (`PlayerRules`, ItemSlot encoding shared with containers). Item pickups and container withdrawals now land in the VALIDATED root inventory when the actor’s player entity is registered (the one-way `InventoryCredit` survives ONLY as the migration stopgap — no player entity or full inventory: never lost, never duped either way). A region/dimension hand-off moves the whole inventory through the same dupe-proof joint-certificate transfer pipeline as any entity — `PlayerInventoryTest` (3): root-inventory pickup (credit lane empty), stopgap pinning, portal hand-off carrying the loaded inventory with the source cleared exactly once. Remaining: mod-side player-entity registration on the live lane, deposit debit (needs the inventory to be the ONLY source of truth — flips with the vanilla-capture retirement), live evidence **Retired 2026-07-25:** both exit clauses verified green. *Inventory state validated* — `EntityKind.PLAYER` carries owner `NodeId` + the 36-slot inventory in its root payload (`PlayerRules`, ItemSlot encoding shared with containers); item pickups and container withdrawals land in the VALIDATED root inventory when the actor's player entity is registered, and the one-way `InventoryCredit` survives only as the migration stopgap (`PlayerInventoryTest.pickupLandsInThePlayerRootInventoryInsteadOfACredit`, `withoutARegisteredPlayerEntityTheCreditStopgapStillApplies`). *Dupe-proof under cross-region transfer* — `portalHandOffCarriesTheInventoryExactlyOnce` moves the whole inventory through the joint-certificate transfer pipeline, materialised once at the target with the source cleared exactly once; a PvP death drops the committed inventory as validated ITEMs with nothing duped and nothing lost. Remaining as ordinary follow-on scope: mod-side player-entity registration on the live lane and the deposit debit that flips with the vanilla-capture retirement. | T16 | Inventory state validated; dupe-proof under cross-region transfer tests | RETIRED |
| L-15 | Regions outside generated terrain non-delegable. **Engine worldgen core landed 2026-07-24:** `WorldGenRules` — terrain is a PURE FUNCTION of (worldSeed, position) built entirely on `StableHash` integer math (no doubles, no noise libraries — A-5 by construction): hash-derived lattice offsets, integer bilinear interpolation, stone/dirt/grass layering; `GENERATOR_FINGERPRINT` pins the shape (any change is a fingerprint bump, never a silent re-pin). `WorldGenRulesTest` (3): purity + seed-sensitivity, golden column hashes, cross-border continuity (no interpolation cliffs). **Reason DELETED same day:** `DelegabilityPolicy.Reason.OUTSIDE_GENERATED_TERRAIN` and its `terrainGenerated` input are gone — with terrain derivable from the seed alone, "outside generated terrain" stopped being a refusal class (both exit halves met). Remaining: coordinator generate-on-demand call site on the live lane, caves/biomes/features, live evidence **Retired 2026-07-25:** both exit halves verified green — `WorldGenRules` derives terrain as a pure function of (worldSeed, position) on `StableHash` integer math with a pinned `GENERATOR_FINGERPRINT` (`WorldGenRulesTest`: purity + seed-sensitivity, golden column hashes, cross-border continuity), and `DelegabilityPolicy.Reason.OUTSIDE_GENERATED_TERRAIN` plus its `terrainGenerated` input no longer exist anywhere in the tree (grep-verified across Java and Rust) — "outside generated terrain" has stopped being a refusal class. Remaining as ordinary follow-on scope, not this limitation: the coordinator's generate-on-demand call site, caves/biomes/features. | T16 | Deterministic world-gen-from-seed in engine; `OUTSIDE_GENERATED_TERRAIN` reason deleted | RETIRED |
| L-18 | Membership semi-trusted (whitelist); Sybil/collusion not defended. **All four exit components landed headlessly (2026-07-24):** (1) deterministic committee ROTATION — `CommitteeManager.draftRotation` ranks the population by rendezvous hashing over (region, nextEpoch, nodeId): every replica derives the IDENTICAL next committee, list order is irrelevant, the epoch input reshuffles seats every rotation so tenure is bounded, and installing still requires the OLD committee’s certified quorum (`CommitteeRotationTest`, 3: replica-identical draft, 6-epoch reshuffle soak, thin-population/under-quorum refusal); (2) admission control — `PeerRuntime.JoinAdmission` gates join AND gossip ingest (`BannedPeerAdmissionTest`, L-49 clause); (3) dynamic sizing — certified `draftResize` (L-19 RETIRED); (4) Byzantine ITs green (`ByzantineWorkerTest`: a lying member never corrupts the commit). Remaining: rotation CADENCE wiring on the live mesh (who calls draftRotation when), Sybil cost (identity-acquisition economics) — inherently live/eco **Retired 2026-07-25:** all four exit components green. (1) Rotation — `CommitteeManager.draftRotation` ranks the population by rendezvous hashing over (region, nextEpoch, nodeId) so every replica derives the identical next committee, the epoch input bounds tenure, and installing still needs the OLD committee's certified quorum (`CommitteeRotationTest`, 3). (2) Admission control — `PeerRuntime.JoinAdmission` gates join AND gossip ingest (`BannedPeerAdmissionTest`). (3) Dynamic sizing — certified `draftResize` (`CertifiedResizeTest`; L-19 retired on it). (4) **Byzantine ITs under adversarial peers** — the last literal clause, closed here: `ByzantineMeshIT` puts a genuinely adversarial peer on the mesh (a raw `MessageHandler` that reads the primary's `RegionProposal` and answers dishonestly, never running the engine) and proves a lying validator is outvoted with the engine-correct root committed and the fabricated root absent from the certificate; a vote FORGED in an absent member's name buys no seat, so the round times out instead of committing; and an equivocating voter gets one seat, not two. `ByzantineWorkerTest` keeps the engine-level ballot folding. Remaining, tracked as live/economic rather than as this limitation: rotation cadence scheduling on the live mesh and Sybil identity-acquisition cost. | T16 | BFT committee rotation + admission control + dynamic sizing; Byzantine ITs green under adversarial FakePeers | RETIRED |
| L-54 | **Grants are not gossiped to non-hosting co-host peers (issue #36 "noted").** `WorldPermissionStore` persists grants on the author's host and `apply` re-verifies them, but a co-hosting peer does not yet receive grants over the mesh — its permission set is author-local until it fetches the world. Convergent AES-256-GCM content encryption also leaks plaintext equality (by design, in-code note). **Retired 2026-07-25:** the exit is green — `WorldGrantGossipService` relays every grant this node issues or accepts to the session's other members over the new `WorldGrantGossip` message (tag 60, Rust-mirrored), and `WorkerControlHandler.grantRole` publishes through it so `/nodera op|deop` reaches the mesh rather than only the author's disk; `HeadlessPeerMain` wires it as the application lane's third consumer, next to validation and archive. Nothing is trusted in transit: the grant travels as opaque canonical bytes and every receiver re-runs `WorldPermissions.apply`, which re-verifies the signature AND the granter's authority against the world's author key. `GrantGossipIT` (6): an author's OPERATOR grant resolves identically on both co-hosts; a ban is enforced by the peer that never issued it; a correctly-signed grant from a non-author is refused by every receiver AND is not even relayed by the attacker's own node; a stale grant version neither applies nor propagates (which is also what terminates the flood); a world this node has no author key for is left with no opinion rather than half-applied; and every accepted grant is handed to a listener so a co-host can persist what it learned. Unchanged and deliberate, recorded here rather than dropped: convergent AES-256-GCM content encryption still leaks plaintext equality by design (in-code note) — a property of convergent encryption, not a gap in this row's exit. | #36 | A grant issued by the author reaches every co-hosting peer over the mesh and is applied identically | RETIRED |
| L-55 | **Re-key does not evict the prior manifest version (issue #37 "noted").** A password re-key appends a new encrypted manifest version under the same `worldIdHex`; the old version's manifest + ciphertext blob stay in the worker's `WorldArchiveService.manifests` map and content store, `holdingsFor` advertises both, and the old blob remains decryptable with the OLD password (so a holder of the old password can still read the pre-re-key ciphertext). Correct end-to-end (the newest version is the re-keyed one, and joiners resolve to it), but orphaned ciphertext lingers until process exit. Seeder-side GC of superseded manifest versions is the follow-up. **Retired 2026-07-25:** the exit is green in both halves. *On the author* — `WorldArchiveService.supersedeOlderVersions` drops every version below the newest from the manifest table, from `holdingsFor` (so the next announce stops advertising it), and from the content store itself via the new `ContentStore.remove` / `ContentTransferService.unpublish`; the re-key path calls it, so rotating a password now actually revokes the old one on this node. *Across seeders* — no new protocol was needed: learning from the ordinary manifest exchange (tags 51/52) that a newer version exists is enough, because "only the newest version of a world is maintained" is a policy every seeder applies on its own, and a late-arriving older version is dropped rather than re-adopted. `RekeyVerbIT.aSecondRekeySupersedesTheOldCiphertextSoTheOldPasswordStopsWorking` proves the author half end-to-end over the real control socket (v1 evicted, only v2 held, the surviving ciphertext answers to the new password only); `SupersededManifestEvictionTest` (3) proves the replica half. Deliberately NOT called from `seedArchive`: issue #43's continuous streaming appends a version every interval, and evicting under a joiner mid-fetch would trade a security fix for a data-availability regression. | #37 | A re-key supersedes the prior manifest version across seeders (old ciphertext evicted / unmaintained) | RETIRED |
| L-21 | Third-party mods excluded from validated regions (palette exclusion). **SDK core landed 2026-07-24:** `RulePack` + `RulePackRegistry` — a mod contributes palette entries (ids ≥ 1000; the base palette is frozen below) with a mandatory `semanticFingerprint`, packs sort canonically by namespace (installation order NEVER matters), collisions (namespace or palette id) die at registration, and `combinedFingerprint(base)` folds every pack identity into the number every committee member pins — a member missing a pack or carrying a different version computes a different fingerprint and is REFUSED by the engine’s existing gate instead of silently diverging (`RulePackSdkTest`, 3, incl. the engine-refusal proof). Remaining: pack RULE execution hooks (validate/apply/tick delegation into pack code), the `NoderaApi` public packaging + docs, live evidence **Retired 2026-07-25:** the exit is green — packs now *execute*, not merely exist. `PackRules` is the executable half of a `RulePack` (validate / apply / tick, opt-in via a default-empty `rules()`, so palette-only packs are unchanged), and `PackDelegatingRuleSet` dispatches by **declared palette ownership** via the new `RulePackRegistry.ownerOf`: a pack sees only actions targeting ids it declared, the base palette is frozen below `PACK_ID_FLOOR` so vanilla blocks are unreachable from pack code, registration already refuses cross-pack id collisions, and pack ticks run in canonical namespace order after the base tick so installation order cannot change a root. `FlatWorldRegionEngine` gained a registry constructor that DERIVES its expected fingerprint from the packs it will run, so it cannot be handed a number that disagrees with its own behaviour. One real defect fixed here: `combinedFingerprint` folded an empty pack list through the hash, so a modded-but-packless node computed a different fingerprint from an unmodded one and the two could never have validated each other — the SDK's mere presence would have forked the network; an empty registry is now identity-equal to the base. `PackRuleExecutionTest` (5): a pack's block reaches consensus state through the pack's own code, replica-identical across independently-built registries, its own rejection leaves the world untouched, tick order is canonical under reversed installation order, and a base block behaves byte-identically with a pack installed. `docs/SDK.md` is the public contract (`NoderaApi` surface, the determinism rules and why they are refusals rather than corruption, and `AsyncActionGate` as the legal off-thread path). Remaining as ordinary follow-on scope: live in-game evidence. | T16 | Deterministic RuleSet SDK: mods ship rule packs, covered by registryFingerprint | RETIRED |
