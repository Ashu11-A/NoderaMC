# App — Retired Limitations

<!-- AI-AGENT-INSTRUCTION: Rows arrive here from LIMITATIONS.md when their EXIT TEST is green, WITH
     the evidence that retired them. Never delete a row or soften an evidence cell. If a retired
     capability regresses, open a bug issue and add a NEW row to LIMITATIONS.md — do not edit history
     here. -->

**Category:** app · **Last audit:** 2026-07-28 · Retired rows: **7**

| ID | Limitation | Retirement evidence | Owner | Retired |
|---|---|---|---|---|
| A-UX-1 | When the link to the worker went down, only the top bar changed: every screen kept rendering its numbers unmarked, because they all gate on `link.has_data`, which stays true. Keeping the last picture is deliberate; failing to say it is the last picture is not | `api.ts` gained `isStale(link)` — a picture exists (`has_data`) but the status is neither `live` nor `polling`. Both halves are load-bearing: without `has_data` an app that never connected would call its em-dashes stale, and without the status check a live app would call live numbers dated. The desktop shell (`App.tsx`) renders `StaleDataNotice` above every screen that shows worker-derived figures — Overview, Worlds, a single world, Peers — so no such screen can be added without a staleness decision; Settings and About are excluded because they describe the app, not the node. The phone shell mirrors it on both figure-bearing tabs with the same sentence, `Showing the last known picture — the worker link is offline.` Exit test `a screen showing worker figures marks them as last-known when the link is down` (ui/tests/ux-honesty.test.mjs) pins the predicate, both shells, and the shared wording; green on 2026-07-28 | [10](Task.10.md) | 2026-07-28 |
| A-UX-2 | `appearance.notifications` reported itself as in force and had no read site — no consumer, no `tauri-plugin-notification`, no capability entry. `Enforcement::Local` resolved to `Live` unconditionally | Declared unenforced, which is the second branch of the exit test and the honest one for this build: nothing raises a notification, so reporting `Live` was exactly the defect the enforcement table exists to prevent. `ENFORCEMENT` now carries `Enforcement::Never { reason: "desktop notifications are not wired up in this build; the toggle is saved but no notification is raised" }`, and the Settings toggle renders `note("appearance.notifications")` so the reason is on screen next to the control. Following the L-56 precedent the toggle is kept rather than removed, so a preference users already saved is not silently dropped. `appearance_notifications_is_declared_unenforced_with_a_reason` (nodera-app, 187 tests) asserts a worker confirming the key cannot make it live, and `the notifications toggle is badged with why it is not in force` asserts the badge is actually rendered; both green on 2026-07-28 | [10](Task.10.md) | 2026-07-28 |
| A-UX-3 | Saving a restart-scoped setting told the user to restart the worker themselves, while the app already owned the restart signal | Verified rather than rebuilt: the Settings restart banner already branched on `WorkerOwnership.can_restart`. When the app owns the worker it offers a `Restart worker` button that calls the `restart_worker` command directly; in attach mode — where the process belongs to `scripts/dev.sh` or an operator — it says `Restart it where you started it.` rather than offering a button that would kill someone else's process, which is the "explains why it cannot in this mode" half of the exit test. What was missing was a check keeping it that way. `the restart banner restarts the worker, or says why it cannot` (ui/tests/ux-honesty.test.mjs) asserts both branches inside the banner block; green on 2026-07-28 | [10](Task.10.md) | 2026-07-28 |
| A-UX-5 | Six registered commands had no desktop caller: `settings_fault`, `pause_reason`, `dashboard_world`, `open_share_file`, `get_unenforced_settings`, and the `nodera://pause` event | Each was resolved on its merits rather than uniformly. **Given a caller:** `pause_reason` — Overview now fetches it while transfers are paused and prints the worker's own words, because a node that quietly stops seeding on mobile data looks identical to one that crashed; `settings_fault` — the desktop Settings screen now shows the same warning the phone screen always did, that these are defaults and the real document is on disk untouched, so nobody re-enters a configuration on top of a file that was never the problem. **Deleted:** `dashboard_world` (the pushed dashboard already carries every world), `open_share_file` together with its now-unreachable `read_share_file` helper, and `get_unenforced_settings` with its `Settings::unenforced` shim — a flat list of key names structurally cannot say *why*, which `get_setting_status` does. The `nodera://pause` event was a second channel for a fact the pushed dashboard already carries; the tray now just requests a push, so there is one truth and one path. Two exit tests hold the line: `every registered command has a frontend caller` walks all 47 names out of `generate_handler!` and every `.ts`/`.tsx` file under `src/`, so a future orphan fails the build rather than accumulating, and `the six A-UX-5 commands are each resolved, and stay resolved` names all six individually. Both green on 2026-07-28, alongside 187 `cargo test` cases in nodera-app | [10](Task.10.md) | 2026-07-28 |
| A-UX-4 | The tracker-stores screen used colour utilities that Tailwind did not generate and had no desktop page padding; its first repair then forced the desktop palette through the Material 3 mobile shell | `TrackerStores.tsx` now consumes shell-semantic custom properties. Desktop maps them to the generated app palette; mobile maps every control/card/dialog role to generated Material 3 `primary`, `surface-container-*`, `outline-*`, `error-*`, and `scrim` properties, so a source-colour change repaints the whole screen. `App.tsx` retains the established desktop `max-w-[1100px] px-[26px] pt-5 pb-10` frame while mobile selects `shell="mobile"` inside its existing `px-4` frame. `built tracker-store CSS resolves desktop and mobile shell roles` runs after Vite and verifies those emitted CSS declarations and both generated padding selectors; green on 2026-07-28 | [10](Task.10.md) | 2026-07-28 |
| L-47 (first form) | There was no automated job that built the app, exercised the mod's presence gate in both directions, or proved a hosted world survived its game process — every one of those claims rested on a manual run | The companion CI job builds the Tauri app end to end (UI build plus a cargo release, with the worker distribution staged and bundled), runs the gate **both ways** (`CompanionGateTest`: worker present ⇒ Minecraft starts; worker absent ⇒ an actionable abort), and proves hosted-world survival via `WorldContinuityIT` and `CompanionCrashSurvivalIT` — the latter keeping the daemon seeding after a co-located game process is SIGKILLed with no shutdown hook run. En route the job surfaced and fixed four packaging gaps that only a clean-checkout build could reveal: distribution staging, a gitignored UI build, gitignored bundle icons, and a gitignored tray icon. **A narrower row of the same id remains open** for the genuinely cross-machine half — installer-based install plus a join from a second, separately networked side | [4](Task.4.md) | 2026-07-23 |
| L-78 | The app could not ask the telemetry question, and its "what is collected" disclosure had a fallback that could lie by being stale | The consent modal and Privacy screen landed earlier; the half that stayed open was the disclosure's offline behaviour, and it was worse than documented — the code returned an **error**, so a person asking "what is collected?" with an unreachable collector got a blank screen at exactly the moment they were deciding whether to consent. The registry now lives in the `nodera-telemetry` **library** (`schema::schema_json(source)`) instead of the binary, so the ingest service and the app share one source rather than two hand-maintained copies of a privacy notice — the drifting one always being the copy people read. The app depends on the crate by path and falls back to it, and every copy carries `registry_version` (the repository `VERSION`, stamped by `build.rs`) plus `disclosure_source` (`service` vs `bundled`), so a stale copy is **visibly** stale and the screen can label a fallback as one. `the_disclosure_falls_back_to_the_bundled_registry_when_the_service_is_unreachable` and `the_bundled_copy_says_it_is_bundled` (nodera-app, 63 tests) are the exit clause; `TelemetryRegistryMirrorTest` still passes against the real binary, so the Java emitter and the Rust registry have not drifted. An unconfigured collector stays an error — nothing to disclose is not the same as cannot reach | [5](Task.5.md) | 2026-07-26 |
