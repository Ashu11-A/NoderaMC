# Engine — Retired Limitations

<!-- AI-AGENT-INSTRUCTION: Rows arrive here from LIMITATIONS.md when their EXIT TEST is green, and
     they arrive WITH the evidence that retired them (test names, what changed, what was deliberately
     left as follow-on scope). Never delete a row from this file and never soften its evidence cell:
     this is the audit trail that lets a future reader check a retirement instead of trusting it. If
     a retired capability regresses, open a bug issue and move a NEW row into LIMITATIONS.md — do not
     edit history here. -->

**Category:** engine · **Last audit:** 2026-07-28 · Retired rows: **22**

Rows moved here from [`LIMITATIONS.md`](LIMITATIONS.md) §B when their status reached `RETIRED`.

> **Audit 2026-07-30 — L-20's retirement does not hold, and the live claim now sits in
> [`LIMITATIONS.md`](LIMITATIONS.md) as L-92.** Its evidence cell below is left exactly as written,
> per this file's own rule: it is the audit trail, and softening it would destroy the record that
> makes the mistake checkable. The mistake: `GenesisApprovalFlow` and `GenesisRecertification` are
> complete and tested and have **no production call site** — `:peer:structureReport` lists the flow
> under "classes only tests and benchmarks reference", and `GenesisApprovalFlowIT` is its only
> construction site. Meanwhile `CertifiedWorldGenesis`, the record production actually mints and
> persists, carries one author key and one signature and no founding set at all. So the row was
> retired on a mechanism no world has ever used, and genesis is still the single-signer trust root the
> row describes. This is the same defect shape the 2026-07-29 network audit found in L-87/L-88 and
> L-33 — green tests over an unreachable class — and it is worth noting that it can reach a *retired*
> register, where nobody re-checks. The blocker is a design decision (what the declared founding set
> is, and how it is persisted with the world), not a missing call; see L-92 for the exit test.
>
> **The same sweep found a second, milder case: L-25's two halves are not both wired.** That row
> retired on "a documented rejection *and* a legal API", and its own evidence cell says the thing that
> had been missing was a call site — "a guard nothing calls rejects nothing". The guard's call site is
> real and the safety property holds: `LevelChunkMixin` → `BlockWriteGuard` routes every block write,
> and an off-thread write is rejected with `AsyncWriteException`. But the **legal API** that exception
> names, `AsyncActionGate.submit`, has zero production references — nothing constructs the gate,
> nothing drains its FIFO. So a third-party mod that does exactly what the error message tells it to
> do has no working path. The row's evidence stands as written (it is accurate about the guard); the
> unwired half is carried by **L-93** in [`LIMITATIONS.md`](LIMITATIONS.md).

| ID | Limitation | Retirement evidence | Owner | Retired |
|---|---|---|---|---|
| L-3 | Gravity blocks and fire excluded from the validated lane | `GravityRules` instant-settle (broken support drops the contiguous stack bottom-first; strictly vertical, so no border signalling) plus fuel-bounded fire in `RandomTickRules` (1-in-3 extinguish, 3×3×3 spread, always dies out). `GravityFireRulesTest` (5): instant landing, column drop, fuel-only spread with guaranteed burnout, 300-tick blaze with replica-identical roots | [10](Task.10.md) | 2026-07-24 |
| L-4 | No deterministic lighting, so light-dependent mechanics could not validate | `LightField` computes sky and block light as a **pure function of committed block state** — light is derived from the root, not stored beside it, so there is nothing to drift. Sky down-beam with lateral leak, emitter BFS around walls, opacity whitelist; grass ticks and the spawn lane read the same API. `LightFieldTest` (6): golden sky/overhang/roof values, per-hop decay, around-walls, purity | [10](Task.10.md) | 2026-07-24 |
| L-5 | Observer and quasi-connectivity missing; palette v2 semantic gaps | Observers pulse 15 out the back for exactly one tick with **both edges in the hashed queue**, so the pulse is consensus phase; quasi-connectivity reads the cell above, so classic BUD staleness emerges from the scheduling model rather than a special case. `ObserverQcTest` (5) | [9](Task.9.md), [10](Task.10.md) | 2026-07-24 |
| L-6 | Daylight- and time-coupled blocks excluded | `committedWorldTime` rides `RegionExecutionContext` as a member-agreed, root-determining input (transient in `MutableRegionState`, never hashed directly); `DAYLIGHT_SENSOR` emits 15 while exposed to open sky in daytime — a state **and** time dependent source special-cased in the BFS seeding. `DaylightSensorTest` (3) | [10](Task.10.md) | 2026-07-24 |
| L-9 | Projectiles, TNT, and rails/minecarts not validated | All three are engine-owned validated state with replica-identical determinism fixtures: TNT (seeded crater, fuse, chain ignition, knockback), projectiles (Q32.32 ballistics, ≤ 1-block sub-step march, entity hit), minecarts (rail-graph kinematics by connectivity, redstone-gated boost). `TntRulesTest`, `ProjectileRulesTest`, `RailRulesTest` | [11](Task.11.md) | 2026-07-24 |
| L-10 | Comparators, hoppers, and note blocks outside every lane (needed containers) | Container contents entered the hashed root (`ContainerEntry` tag 105, snapshot body v4, delta body v5 with replace semantics); `CHEST` + `ContainerAction` gave committee-validated deposit/withdraw checked against kind, slot bounds, reach and **current contents**; `HOPPER` self-schedules an 8-tick cycle on the hashed queue; comparators emit container fill as a state-dependent directional source that re-settles reactively on every container mutation. Delegability is palette-driven, so container regions are delegable. `ContainerStateRootTest` (5), `ContainerRulesTest` (7). Chest GUI and live evidence remained as mod-side presentation scope, not a validated-state limitation | [12](Task.12.md) | 2026-07-25 |
| L-11 | Player inventory outside the region root (pickup was a one-way credit) | `EntityKind.PLAYER` carries owner and the 36-slot inventory in its root payload; pickups and container withdrawals land in the validated root inventory, and the one-way credit survives only as the migration stopgap. `PlayerInventoryTest.portalHandOffCarriesTheInventoryExactlyOnce` is the dupe-proof cross-region clause: removed at source, materialised exactly once at target | [12](Task.12.md) | 2026-07-25 |
| L-13 | Combat, health, and XP not validated | Every damage source routes through the single `MobCombatRules.damage` mutation point (arrows, blast falloff with integer linear falloff, player melee via `AttackEntityAction` with committee-validated reach and a rule-set damage constant — a modified client cannot hit harder or farther). Vitals are the entity's canonical `[u16 health][u16 maxHealth]` root payload. The clause actually missing was `@Invariant(10)`: `CombatStateRootTest` (4) proves identical blocks with different health produce **different** roots, and that a dead mob is *absent* from the root rather than present at zero. XP and mob drops remained ordinary follow-on scope | [12](Task.12.md) | 2026-07-25 |
| L-14 | Portals, dimensions, and commands outside the validated path | Portals: cross-dimension travel **is** generalized region transfer — `PortalRules` emits a transfer intent whose target region is in the nether (8:1 fixed-point scale, no new protocol). Commands: `CommandAction` (tag 108) + `CommandRules` with **engine-side** authority against a committee-agreed operator set on `RegionExecutionContext`, no minting of states a player could not place, volume-bounded fills iterating in canonical (y, z, x) order, and `/time set` refused because committed world time is a context input rather than region state. `CommandSubsetTest` (8) | [12](Task.12.md) | 2026-07-25 |
| L-15 | Regions outside generated terrain non-delegable | `WorldGenRules` derives terrain as a pure function of (worldSeed, position) on `StableHash` integer math — no doubles, no noise libraries — with a pinned `GENERATOR_FINGERPRINT`. With terrain derivable from the seed alone, `DelegabilityPolicy.Reason.OUTSIDE_GENERATED_TERRAIN` and its `terrainGenerated` input were **deleted** (grep-verified absent from both languages). `WorldGenRulesTest` (3) | [12](Task.12.md) | 2026-07-25 |
| L-18 | Membership semi-trusted; Sybil and collusion undefended | Four components: deterministic committee **rotation** by rendezvous hashing over (region, nextEpoch, nodeId) so every replica derives the identical next committee and tenure is bounded (`CommitteeRotationTest`, 3); **admission control** gating join and gossip ingest (`BannedPeerAdmissionTest`); certified **dynamic sizing** (`CertifiedResizeTest`); and the clause that was genuinely unmet — **Byzantine ITs under adversarial peers**: `ByzantineMeshIT` (3) puts a real adversarial peer on the mesh (a raw handler that reads the primary's proposal and answers dishonestly, never running the engine), and proves the fabricated root never reaches a certificate, a forged vote in an absent member's name buys no seat, and an equivocator gets one seat rather than two | [12](Task.12.md) | 2026-07-25 |
| L-19 | Small populations degrade quorum (3-of-4 → 2-of-3 degraded mode) | `CommitteeManager.draftResize` makes every sizing transition a certified reconfiguration: the new committee's quorum is a strict majority of its actual size, installing requires the **old** committee's quorum of approvals, and `isDegraded` is now derived from the certified committee (the static flag was deleted). `CertifiedResizeTest` | [12](Task.12.md) | 2026-07-23 |
| L-20 | Genesis is a single-signer trust root | `GenesisRecertification` (tag 104): the founding set endorses the root with a strict-majority quorum of distinct founder signatures over (root ‖ complete founding set). `GenesisApprovalFlow` + `GenesisApprovalRequest`/`Grant` collect it: the host pins root and declared set, self-approves, asks founders as they join, and verifies every grant against the **declared** founder key. A founder signs only a root it can vouch for (fail-closed predicate). `GenesisApprovalFlowIT` — outsider, forged, and duplicate grants never count | [12](Task.12.md) | 2026-07-24 |
| L-21 | Third-party mods excluded from validated regions (palette exclusion) | `PackRules` is the executable half of a rule pack (validate/apply/tick, opt-in via a default-empty `rules()`); `PackDelegatingRuleSet` dispatches by **declared palette ownership**. Safe by construction: the base palette is frozen below a pack-id floor, registration refuses cross-pack id collisions, pack ticks run in canonical namespace order, and the engine **derives** its expected fingerprint from the registry it will run. Writing the test caught a real defect — `combinedFingerprint` folded an *empty* pack list, so a modded-but-packless node computed a different fingerprint from an unmodded one; shipping the SDK without the fix would have forked the network. `PackRuleExecutionTest` (5); public contract in [`SDK.md`](SDK.md) | [12](Task.12.md) | 2026-07-25 |
| L-22 | A fixed spot-check floor costs ~12.5% re-execution forever | `SpotCheckPolicy` samples adaptively by committee reliability (N = 4 below the assignment floor, 8 mid, 64 at ≥ 0.99), and the exit is **measured**: 120k committed versions across 3 regions × 2 server secrets hold the proven-committee re-execution share at ≤ 2% (empirically ≈ 1.56%), with the ~25% floor pinned for unproven committees | [5](Task.5.md) | 2026-07-23 |
| L-26 | Redstone bounded to palette v2 | Three staged component groups, all green. The gap was stage 1: two of palette v2's nine named components were missing. **Pressure plates** couple the entity lane to the redstone lane — a plate answers to where something is *standing*, so it only became expressible once entities were validated root state; it releases after 20 ticks through the **hashed** queue (the delay is consensus state and survives a delta boundary), ghosts never press one, and a pressed plate is unplaceable. **Sticky pistons** made every helper family-aware and share the entire extend path, differing only on retraction, where every failure mode fails closed and the piston still retracts. `RULES_VERSION` 3→4, palette literal `palette.v4`. `PressurePlateStickyPistonTest` (7), including the order-independence root property | [9](Task.9.md) | 2026-07-25 |
| L-8 | Mob spawning cycles were vanilla-only: the engine had no spawn cycle of its own, so nothing about where or how often hostiles appeared was validated | The deterministic cycle is landed and its exit clause — *engine light + seeded RNG matching the vanilla rate envelope* — is now asserted rather than described. `SpawnRulesTest` pins all three halves: **light** (a lit platform spawns zero over 2000 ticks, and the gate reads the engine's own committed `LightField`), **seeded RNG** (2000 ticks of cycles settle to one identical root on repeated replicas), and the **rate envelope** (one attempt per `SPAWN_INTERVAL_TICKS`, at most one spawn per attempt: after k intervals the population cannot exceed k, and 100 attempts fill to `MOB_CAP` and stop). Spawns enter the root as GHOSTs with deterministic ids, full vitals, and a 6000-tick despawn horizon. **Not covered here, deliberately:** suppressing the *vanilla* spawner in a delegated region is the live mirroring half, tracked by L-24 and the live lane — this row was about the engine having no cycle at all | [11](Task.11.md) | 2026-07-25 |
| L-25 | Async world writes by other mods are undefined under the guard | Both halves had existed for a while — the legal API (`AsyncActionGate.submit`: signed actions from any thread into a bounded FIFO, drained once per tick on the server thread, so asynchrony ends at the gate and determinism begins at the drain) and the documented rejection (`MutationGuard.verdictChecked` throwing `AsyncWriteException` with a message naming the gate and `SDK.md`). What was missing was a **call site**: a guard nothing calls rejects nothing. `LevelChunkMixin` — the single `setBlockState` choke point — now routes every block write in the game through `BlockWriteGuard`, which picks the entry point **by thread**: a main-thread write is classified (`verdict`), and a write arriving from any other thread gets `verdictChecked` and the documented exception, rethrown rather than degraded so the offending mod actually sees it. Choosing by thread rather than by source is deliberate: an unlabelled MAIN-thread write is ordinary vanilla plumbing, and rejecting those would have turned the guard into a crash generator. `MutationGuardTest` keeps the semantics pinned headlessly | [12](Task.12.md) | 2026-07-25 |
| L-24 | The `mobCapture` ghost lane shipped default-off because nothing had been proven; every capture needed an explicit dimension opt-in | The row's exit is `e2e-mobs.sh` G2a **and** G2b, and both pass on a dispatched live run (30196607049). **G2a**: a zombie summoned in a dimension that opted nothing in is captured on the species default alone — the engine owns its behaviour, so the lane keeps it without being asked. **G2b**: a species the engine does not own still refuses its region, names the reason, and announces the refusal to the mesh. Getting there took the whole minecraft L-60 investigation (see its evidence) plus correcting this stage's own assertion: G2a used to require that NO revocation followed the zombie summon, which fails whenever the lane correctly refuses a region for one of the nether's own striders or magma cubes. It now asserts what it means — that no revocation names the zombie | [11](Task.11.md) | 2026-07-26 |
| L-52 | Fixed-point multiply, immutable entity-motion copies, and packed chunk-coordinate keys were duplicated across engine rules/state views | One implementation now owns each operation: `FixedPoint.multiply`, `PersistedEntityState.withMotion`/`withMotionAndAge`, and `ChunkKey.pack`/`unpackX`/`unpackZ`. Source audit leaves exactly one `Math.multiplyHigh` and one chunk-pack bit layout in Java; all equivalent entity reconstructions named by the row route through the copy API. The original row incorrectly named `WorldMutationApplier` (it uses a coordinate record, never a packed key) and a local `MobAiRules.multiplyFixed` (it used `FixedVec3.scale`); `FixedVec3.scale` now routes through `FixedPoint`, so its arithmetic is centralized too. `FixedPointTest` checks 10,000 arbitrary signed pairs against `BigInteger`; `ChunkKeyTest` checks 10,000 signed pairs and exact bit layouts; `EntityLaneTypesTest.motionUpdateMatchesDirectConstructionCanonicalBytesAndRoundTrips` proves byte identity and decode equality. Focused core/engine XML: 767 tests, 0 failed/skipped; full `./gradlew check`: 2,061 tests, 0 failures/errors | [2](Task.2.md) | 2026-07-28 |
| L-51 | Cross-region fluid inflow sampled only dense-section cell `(0,0,0)`, so off-corner edge fluid never scheduled its adjacent owned cell | `FluidRules.seedBorderInflow` scans the complete ownership-facing 16×16 face for uniform and dense sections in canonical column/section/y/offset order, skips diagonals, indexes scheduled positions once, and derives cadence from `desiredAt`'s winning fluid. Standard-shape dense work is bounded at 196,608 candidates rather than 884,736 probes. `CrossRegionFluidTest` (9) pins blocked-first uniform recovery, side-only dense behavior, diagonal rejection, mixed-fluid cadence, and the exact off-corner `ScheduledTickEntry` on two replicas with identical roots. Focused class and full `./gradlew check` green. Certified halo delivery remains separate L-2 scope | [10](Task.10.md) | 2026-07-28 |
| L-17 | Gateway migration is a brief reconnect plus an action freeze: when the gateway a player's actions flow through went away, the actions in flight were silently dropped and the world stopped moving until the reconnect completed | The exit clause — *local-replica world view: play continues with zero reconnect during migration* — is driven end to end headlessly by `GatewayMigrationContinuityIT` (4 tests, 0 failures, re-run with `--rerun-tasks --no-build-cache`): a player submits, the gateway dies mid-swing, the player keeps playing while the session has **no gateway at all** (the render never goes blank because `LocalReplicaView` is fed by committed state plus this player's own predictions and never by the gateway), and when a link to the new gateway comes up every action reaches it while the ones the old gateway already acknowledged are not replayed. Both halves are asserted together on purpose: rendering by dropping actions is not continuity, and preserving actions by freezing the screen is not either. `GatewayHandover` (12 tests) is the freeze/replay core — queue rather than refuse, replay in submission order, exactly once by `(actor, playerSeq)` identity, byte-identical envelopes so a migration cannot mint authority — and `GatewayHandoverListener` (3 tests) binds it to gateway re-election, resuming immediately when this node wins and staying frozen when it does not. **What this retirement added, and why the row was previously held back:** the row was kept at RETIRING because the mod's capture path did not route through the handover. It does now — `LiveEntityLaneRuntime.submit` offers every signed action to `handover` and acknowledges on send, and `NoderaPeerService.clientGatewayListener` registers the listener on the client peer runtime. The audit for this retirement found the wiring was still **half** done: `submitAsObserver` — the route every action takes for a region this node does not own, which under field-of-view ownership is the common case — bypassed the handover and the prediction feed entirely, so a migration still ate the swing everywhere outside a player's own regions. It now takes the same two overlays, and `replayAsObserver` resolves the replay target from the ownership plan because `forwardToPrimary` starts from a local lease an observer does not have. `GatewayMigrationContinuityIT.theObserverRouteFreezesAndReplaysExactlyOnce` pins it, including the case that matters most: a resume while the primary is still unreachable consumes nothing, so the actions stay held for the next one | [12](Task.12.md) | 2026-07-28 |
