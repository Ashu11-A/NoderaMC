# Minecraft — Retired Limitations

<!-- AI-AGENT-INSTRUCTION: Rows arrive here from LIMITATIONS.md when their EXIT TEST is green, WITH
     the evidence that retired them. Never delete a row or soften an evidence cell — this is the audit
     trail that lets a reader check a retirement instead of trusting it. If a retired capability
     regresses, open a bug issue and add a NEW row to LIMITATIONS.md; do not edit history here. -->

**Category:** minecraft · **Last audit:** 2026-07-25 · Retired rows: **3**

| ID | Limitation | Retirement evidence | Owner | Retired |
|---|---|---|---|---|
| L-31 | The in-game diagnostics HUD shipped its session and network panels, but the region-ownership and entity panels rendered **placeholders** — the providers behind them were stubs, so the most useful panels showed nothing regardless of what the node was doing | Both data halves exited with **live** evidence. The entities panel is fed by a live entity-control provider — observed live at 239 entities across 12 delegated regions with versions advancing every flush. The region-ownership panel is fed by a live ownership provider — observed live at `14 owned / 896 owned chunks`, with the unassigned placeholder retired. While any validation lane is active the panel shows the node's real leases (owned / validating / replica, with chunks, epoch, and expiry); the empty placeholder renders **only** when no lane is active, which is correct behaviour rather than a gap. The stub provider was replaced by the live singleton registration | [3](Task.3.md) | 2026-07-24 |
| L-45 | **No automated real-client acceptance in CI.** Dev runs launched from Gradle and eight scripted suites drove real clients by hand, but nothing ran them under a headless display, so every live exit in this repository was blocked on a human running a script | The `e2e-live` workflow is **green** under Xvfb on GitHub's two-core runners ([run 30166012788](https://github.com/Ashu11-A/NoderaMC/actions/runs/30166012788)): the continuity series (share → player B in-world → archive on the network → **host killed** → player B recovers and re-hosts) plus the crash suite, the clean-slate pickup drive, the full `/nodera` command sweep with the in-game selftest, the far-apart ownership interrogation, and ownership-follow. Getting there cost three real defects, each invisible on a developer machine: a fresh game dir puts the **accessibility onboarding screen in front of quick play** (every scripted client sat on it for the whole timeout — the stall reporter named it); a killed dedicated server holds **RCON open while it saves**, so the next suite's preflight failed on a healthy stack; and RCON is answered on the server thread, so generating terrain 200 km out (274 ticks behind) made a position read come back **empty** rather than late. The suites now seed first-run options, wait for their own listeners, poll instead of sampling, and fail fast when a quick-play client parks on a screen quick play never reaches | [1](Task.1.md) | 2026-07-25 |
| L-52 | **No password gate on the live Minecraft join path.** The world password protected the archived content plane, not who connects: a listed world's game route is public, so anyone who resolved it could play in a "password-protected" world | A configuration-phase challenge-response, so a refused joiner never becomes a player: the host derives a gate key from the password once (random salt, the content plane's KDF and cost), each connection gets a fresh nonce, and the joiner answers `HMAC(gateKey, ctx ‖ nonce ‖ worldId)`. Bound to nonce **and** world; single-use; a bounded, expiring challenge store; and an arming failure **seals** the world rather than opening it. `JoinPasswordGateTest` (8) + `HostJoinGateTest` (11) pin the rule headlessly. Live exit green in CI ([run 30166646528](https://github.com/Ashu11-A/NoderaMC/actions/runs/30166646528), `e2e-password.sh`): `PASS W1: refused at the game server — no player, no world, no gameplay`, then the negative control `PASS W2: the correct password joins normally`. `host.sharePassword` gives a dedicated server — which has no Share screen — a way to set one | [5](Task.5.md) | 2026-07-25 |
