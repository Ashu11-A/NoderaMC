# Network — Refactoring Register

Source: `build/jscpd/jscpd-report.json` (filtered to `library/java/transport`, `library/java/storage`,
`peer`, `library/rust/nodera-codec`) + manual review of god classes, long methods, and dead code.
Line counts from `build/loccount.txt`. Audited **2026-07-28**. Scope: the four modules this
category owns; `build/`/`target/` and generated code are excluded. `% duplicated` is
`(Σ duplicated line-runs ÷ file lines) × 100`, so a small file flagged against several partners
can exceed 100 %.

> This register records candidates and a sequencing rationale only. No code is changed here —
> every row is evidence for a future refactor branch, and rows that touch the frozen wire
> (`MessageCodec`, `WireRegistry`, deleted tags) must clear the cross-language fixture and tag-
> mirror gates in the same commit.

## Duplication and structural candidates

| File | Lines | % duplicated | Duplicated-with | Refactor plan |
|---|---:|---:|---|---|
| `transport/.../protocol/EchoTest.java` | 29 | 458.6 | `core/.../action/ActionEnvelope` (+ many) | **Dead code.** Tag 17 has a decoder but no production sender (REFERENCE §11 #9). It is the single largest source of cross-file duplication noise. Remove the runtime class; the frozen tag number is retired, not reclaimed. |
| `transport/.../protocol/content/ArchiveReplicaAck.java` | 59 | 125.4 | `ArchiveReplicaAssignment` | **Dead code.** Tag 31; `ArchiveRepairService` re-audits rather than trusting acks, and no path sends it (REFERENCE §11 #10). Remove; mirror stays in Rust for the frozen registry. |
| `transport/.../protocol/content/ArchiveReplicaAssignment.java` | 62 | 71.0 | `ArchiveReplicaAck` | **Dead code** (tag 30), same row as above. Remove together. |
| `storage/.../storage/CertifiedWorldGenesis.java` | 164 | 118.3 | `WorldAdminProof` | Extract a shared `SealedRecord` helper (canonical bytes + Ed25519 signature + author key) shared by these two and by `PeerCandidate`/`ServiceDirectoryEntry`. |
| `transport/.../protocol/rendezvous/PeerCandidate.java` | 76 | 85.5 | `core/.../action/ActionEnvelope` | Same signed-record pattern as the row above; fold into `SealedRecord`. |
| `transport/.../protocol/discovery/AnnounceEvent.java` | 43 | 83.7 | `CandidateKind` | Two small enums with identical shape; a `WireEnum`-derived template removes the boilerplate (the phase-6 enum machinery already exists). |
| `transport/.../protocol/rendezvous/RelayIncoming.java` | 41 | 75.6 | `content/ManifestHolding` | Small records that differ only in payload type; a generic `TaggedBytes<T>` carrier would collapse them — but only after the TLV plane makes the wrapper free. |
| `transport/.../protocol/rendezvous/RelayConnect.java` | 39 | 74.4 | `PunchSync` | Shared relay-control-record base (`reservationId` + HMAC proof). |
| `transport/.../protocol/rendezvous/PunchSync.java` | 49 | 42.9 | `RelayConnect` | Paired with `RelayConnect`; same shared base. |
| `transport/.../protocol/service/ServiceDirectoryEntry.java` | 67 | 52.2 | `core/.../action/ActionEnvelope` | Third caller of the signed-record pattern; `SealedRecord`. |
| `transport/.../protocol/handshake/ClientHello.java` | 52 | 50.0 | `ChallengeResponse` | **Dead code.** Tags 1–4 were superseded by `Hello`/`HelloAck` (74/75); they exist only in the codec and its tests (REFERENCE §11 #9). Remove runtime + tests; keep the frozen tag numbers. |
| `transport/.../protocol/handshake/ChallengeResponse.java` | 30 | 43.3 | `EchoTest` | **Dead code**, same row (tag 3). |
| `transport/.../protocol/handshake/{ServerHello,WorkerActivation}.java` | — | — | (handshake family) | **Dead code** (tags 2, 4), not jscpd-flagged but listed for the same reason. |
| `transport/.../protocol/RelayEnvelope.java` | — | — | — | **Dead code.** Tag 18 has no runtime sender; the client↔client relay lane was never built (REFERENCE §11 #8). Remove runtime + test. |
| `transport/.../protocol/discovery/InventoryAdvertisement.java` | — | — | — | **Dead code.** Tag 29; the tracker refuses it outright (holdings ride the signed announce), leaving no consumer (REFERENCE §11 #10). |
| `peer/.../diagnostics/metric/TpsMeter.java` | 206 | 48.1 | `TickSkewMeter` | Extract an `IntegerEmaMeter` base (ring + injected clock + basis-point blend). The two meters differ only in what they feed the EMA. |
| `peer/.../diagnostics/metric/TickSkewMeter.java` | 251 | 39.4 | `TpsMeter` | Paired with `TpsMeter`; both are pure-integer, injected-time by category rule. |
| `transport/.../protocol/session/RejectCode.java` | 88 | 47.7 | `wire/WireType` | Both are explicit-code enums; the phase-6 `WireEnum` totality check already covers them — deduplicate the lookup-table boilerplate. |
| `storage/.../storage/event/EventSourcedWorldStore.java` | 74 | 43.2 | `rocksdb/RocksWorldStore` | The shared append guard already exists (`EventChainGuard`); both stores should route every append through it so the duplicated invariants collapse onto one caller. |
| `peer/.../peer/validation/DurableActionJournal.java` | 187 | 39.6 | `DurableInventoryCreditJournal` | Extract a `DurableAppendJournal` base (atomic temp-and-move, fsync, tail read). The two journals are siblings. |
| `peer/.../peer/validation/DurableInventoryCreditJournal.java` | — | 39.6 | `DurableActionJournal` | Paired; same base. |
| `transport/.../protocol/codec/MessageCodec.java` | 1534 | 13.9 | (many) | **God class.** 1,534 lines, 75 `instanceof` arms. Task 14 demoted it to the canonical-only encoder, but the dispatch is still one block. Derive the dispatch from `WireRegistry` (the schema already generates the Rust table) so the Java side is a view over the same rows. Frozen contract — change behind a fixture regen. |
| `transport/.../protocol/wire/InfrastructureCodec.java` | 922 | 15.2 | (many) | 50 TLV shapes, encoder and decoder adjacent. Already one-row-per-message; a code generator off `WireRegistry` would remove the hand-maintenance. |
| `peer/.../peer/PeerRuntime.java` | 888 | 2.7 | — | **God class.** Split membership/gossip, gateway election binding, and message dispatch into collaborators behind the existing `PeerEventListener` seam. Low duplication but high churn surface. |
| `peer/.../peer/discovery/TrackerClient.java` | 754 | 10.1 | — | Split announce / query / catalog / routes / service-directory into separate client classes behind one socket. |
| `transport/.../transport/socket/SocketPeerTransport.java` | 703 | 7.1 | — | Split the authenticated handshake (`TransportAuth`) from the frame read/write loop. |
| `transport/.../transport/rendezvous/RendezvousPeerTransport.java` | 704 | 4.1 | — | Split `TransportSelector` path management from the circuit/reservation lifecycle. |
| One-line Javadoc riding the `SealedRecord` family (Plan.11 phase 2 audit, 2026-08-05) | — | — | `PeerCandidate`, `SignedPeerRecord`, `ServiceDirectoryEntry`, `ServiceObservation`, `ServiceRecord`, `ServiceScore` all repeat `Decode the inverse of {@link #encode(CanonicalWriter)}.`, `@throws IllegalStateException if the tag/version is wrong.` and `@Thread-context one reader per decode call.` verbatim | Confirms, does not add to, the `SealedRecord` rows above — jscpd's clone detector does not strip comments, so these lines are already inside the reported `%`. The fix is the same one: `SealedRecord` collapses the code these records paste, and the matching Javadoc goes with it. No standalone comment-editing pass; a phase-2 agent auditing this table for comment duplication (as opposed to code duplication) would otherwise double-count it as new work. |
| `peer/.../distribution/ContentTransferService.java` | 671 | 6.9 | — | Split the serve path from the download/pacing path; the two share only the budget seam. |
| `storage/.../storage/rocksdb/RocksWorldStore.java` | 512 | 17.2 | `EventSourcedWorldStore` | See `EventSourcedWorldStore` row — route appends through `EventChainGuard`. |

## Unwired capabilities — peer-system review, 2026-07-29

Source: `./gradlew :peer:structureReport` §2.2 ("classes only tests and benchmarks reference"),
cross-checked by grep for non-test call sites. These are **not** duplication candidates: each is a
complete, tested capability with **zero production callers**. This repository's dominant defect is not
broken code, it is unreached code — six instances were found in one sweep on 2026-07-24 and the
handshake was another on 2026-07-29 — so the inventory is kept here rather than left to be
rediscovered.

The distinction that matters per row is **wire it or delete it**. A row that a limitation register
cites as evidence must be wired (a green test over an unreachable class is a false claim about the
product); a row nothing cites can be deleted, and deleting it is cheaper than maintaining it.

| Class | Module | Verdict | Why |
|---|---|---|---|
| ~~`protocol.session.Negotiation`~~ | transport | **WIRED 2026-07-29** | L-87/L-88 cited it as their exit evidence while `PeerRuntime.dispatch` never handled a `Hello`. Now sent, answered and recorded on every announce path (`HandshakeRunsInProductionTest`) |
| ~~`protocol.session.PeerSession`~~ | transport | **WIRED 2026-07-29** | Same triangle: `shapeForEmit` was the R3 mechanism with no caller. `PeerRuntime.sendTo` now encodes through `encodeFor` |
| ~~`distribution.JoinAttemptThrottle`~~ | peer | **WIRED 2026-07-30** | See the security row below |
| ~~`protocol.wire.MessageType.permits` (the authorisation table)~~ | transport | **WIRED 2026-07-30** | See the security row below |
| ~~`peer.sync.EventSyncService`~~ | peer | **WIRED 2026-07-30** | `LiveEntityLaneSession` builds one over its own `RocksWorldStore` and the host transport, answers `EventSyncQuery` on the application-lane arm, and asks its committee peers on open for what each region gained while this node was away. Cold regions are deliberately **not** asked — `lastEventId` is `-1` on an empty log and that request would be one unbounded answer on a frame-bounded wire, so a rejoin syncs forward and a first join takes its world from the archive lane. `EventSyncIsWiredTest` (4) guards the call sites; L-30's citation of `EventSyncOverTransportIT` is now a citation of a mechanism that runs |
| ~~`peer.archival.ArchiveManager`~~ | peer | **DELETED 2026-08-06** (Plan 11 round 2, issue #210) — decided: delete. The row asked which bounding model wins, pieces against `SeedFloorPolicy` or the worker's bytes, and the answer is that only one of the two ever ran. `SeedFloorPolicy` and `CustodyDigest` went with it. Original reasoning: | `assignedManifests` and `reconcile` have no non-test caller, and `WorldReplicationService` already computes placement from `policy.expectedHolders` directly. Two placement implementations, one of which runs. The dead condition inside `reconcile` — both branches of `if (assigned.contains(root))` did the same `retained.put` — is **fixed** on 2026-07-30 rather than left for the verdict: the retained map now starts as a copy of `held` and the assigned set is enforced where it always actually was, in the eviction loop. Note for whoever decides the row: the capability the worker lacks is not placement, it is **release** — the sweep is bounded by a byte budget it never gives back, so a node that filled up on worlds it is no longer placed for stops adopting for the rest of its life. `reconcile`'s cap is expressed in pieces against `SeedFloorPolicy`, a different bounding model from the worker's bytes, so wiring it is a decision about which model wins, not a call site |
| ~~`peer.archival.ArchiveAuditTask`~~ | peer | **DELETED 2026-08-06** (Plan 11 round 2, issue #210) — the row's own verdict, carried out. Original reasoning: | Referenced only by `ArchiveManager`, `ArchiveRepairService` and tests. The audit→repair lane is a closed triangle with no entry point, and the durability property it was written for is **held by the sweep**: placement is a pure function of the live peer set, so a holder's departure re-ranks every world it held and promotes a peer that was not holding it, which is precisely the signal `WorldReplicationService.placedFor` reads. Pinned by `DepartureIsRepairedByPlacementTest` (3) 2026-07-30, so this verdict rests on evidence rather than on reasoning: a departure promotes a non-holder, survivors keep their assignments (rendezvous hashing, not modulo-N, so one departure is not a network-wide reshuffle), and a swarm below the replication factor keeps *every* peer as an expected holder rather than none |
| ~~`peer.archival.ArchiveRepairService`~~ | peer | **DELETED 2026-08-06** (Plan 11 round 2, issue #210) — the row's own verdict, carried out. The tags it would have sent (30/31) stay frozen in `WireRegistry`; only the sender is gone. Original reasoning: | Third member of the same triangle. Also the reason `ArchiveReplicaAssignment`/`Ack` (tags 30/31) are dead in the table above — the sender that would emit them is this. Same evidence as the row above |
| ~~`peer.archival.CustodyAudit`~~ | peer | **DELETED 2026-08-06** (Plan 11 round 2, issue #210) | No caller and no citation found, re-verified 2026-07-30; `CustodyDigest` deleted with it, and `core/region/CustodyClass`'s javadoc no longer claims a spot-check that nothing performs |
| ~~`peer.committee.CommitteeManager`~~ | peer | **DELETED 2026-08-06** (Plan 11 round 2, issue #210) — decided: delete, because the row is right that it is a superseded design and not a missing call site. The runtime `CommitteeChangeCertificate` went with it; its tag stays frozen in `WireRegistry`. Original reasoning: | Verified: the only non-test reference is a javadoc mention in `NodeCapabilities`. [`Task.2.md`](Task.2.md) lists it under "Landed" as the mechanism for authority-free certified committee changes; nothing reaches it. But its model — a nominated committee installed by a `CommitteeChangeCertificate` meeting the *old* committee's quorum — is not the model that shipped. Committees are re-derived deterministically by `EntityLaneBootstrap.plan` from inputs every member holds, on every view change and every region-boundary crossing; there is no rotation event for a certificate to certify. So this is not a missing call site, it is a superseded design, and wiring it would put two rotation mechanisms in one lane. The correction belongs in `Task.2.md` (done 2026-07-30), not in a call site |
| ~~`peer.discovery.BootstrapClient`~~ | peer | **DELETED 2026-08-06** (Plan 11 round 2, issue #210) | Superseded by `PeerDiscoveryService` + `TrackerClient`; no register cited it. `InvitationCodec` and `CachedPeerStore`, which nothing but this resolver read, went with it |
| ~~`peer.discovery.PeerDirectory`~~ | peer | **DELETED 2026-08-06** (Plan 11 round 2, issue #210) — the row's own verdict, carried out, and it cost **four** lanes rather than two: `CachedPeerStore` went with `BootstrapClient`, so `cacheSave`/`cacheLoad` went with `directoryIngest`/`directoryOnline` and `fixtures/bench/baseline.json` lost twelve rows. `DiscoveryBenchmark` is now two benchmarks over code production runs. Original reasoning: | Re-verified 2026-07-30: the only non-test references are a javadoc `{@link}` in `TrackerClient` and `package-info`. Discovery in production is `PeerDiscoveryService` + `TrackerClient` + `RendezvousDirectory`, and none of them consults it. Deleting it costs two of `DiscoveryBenchmark`'s four lanes (`directoryIngest`, `directoryOnline`) and therefore a `fixtures/bench/baseline.json` regeneration — which is the point rather than an objection: `BENCHMARKS.md` currently ranks and load-scales a class production never calls, so half that report measures work no node does. Sequenced with the benchmark regeneration rather than done inside a wiring pass, so the baseline change is reviewable on its own |
| `peer.validation.GenesisApprovalFlow` | peer | **Checked 2026-07-30 — keep; a retired row rested on it and has been reopened** | It was exactly that. Engine **L-20** was retired on this flow plus `GenesisRecertification` (tag 104), and the flow's only construction site is `GenesisApprovalFlowIT`. Meanwhile `CertifiedWorldGenesis` — the record production mints, persists and reuses — carries `authorNodeId`, `authorPublicKey` and one `signature`, and no founding set, so genesis is still the single-signer trust root L-20 called a limitation. Reopened as engine **L-92**, with L-20's evidence left untouched as history (`../engine/LIMITATIONS.fixed.md` carries the audit note). Not wirable as-is: a recertification quorum needs a *declared founding set* persisted with the world, and deciding what that set is — author plus first N joiners? everyone present at first share? — is a design decision, not a call site. Worth noting for future sweeps: this row proves the defect shape can reach a **retired** register, which is the one place nobody re-checks |
| `peer.validation.RegionDelegabilityGate` | peer | **Decide with the engine agent — but note it is a *pair* of unwired implementations** | Re-verified 2026-07-30: its only non-test reference is its own file, and `coordinator.EntityDelegabilityRules` is test-only too. So delegability is decided in production by neither of the two classes written to decide it. Engine L-24's retirement describes the live refusal path (`e2e-mobs.sh` G2b: a species the engine does not own refuses its region and announces the refusal), so *something* refuses — the engine agent needs to name what, and the loser of that comparison is deleted. Flagged rather than decided here because getting it wrong changes which regions can be validated at all |
| ~~`peer.PeerShutdownHook`, `distribution.EmergencyFlush`, `distribution.ActivePlayerStream`~~ | peer | **DELETED 2026-08-04** — the row's own verdict, carried out. `ActivePlayerStream` joined them: 574 lines, zero references outside its own file, and the one place it *did* decide something (two peers committing at the same version returned `CONFLICT` and were silently dropped) is now `RegionMerger`, which merges them instead. Original reasoning: | A shutdown hook nothing installs is a shutdown that does not happen, and installing it needs `EmergencyFlush`, whose `PieceTransfer` seam is a *push*: "take this piece and acknowledge you stored it". Production has no such path — content moves by pull (`ContentTransferService`) — which is also why tags 30/31 are dead, and `LocalHolding` additionally demands the whole contiguous blob in memory with every piece hash re-verified on construction, at shutdown, for a multi-GiB archive. The question that decides the row is not "how hard is the push lane" but **"what restores replication after a departure if nothing evacuates?"** Answer, now pinned by `DepartureIsRepairedByPlacementTest` (3): the sweep does. A departure shrinks the peer set, placement re-ranks, a peer that was not a holder becomes one, and the next sweep adopts. Evacuating on the way out is a second mechanism for a property already held — and a worse one, because it depends on the departing node still being alive and reachable, which a crash and a closed laptop both violate. Delete both rather than build a push lane to feed them |
| `distribution.ChunkLockEditability` | peer | **Blocked on a missing lane, and wiring it today would guard nothing** | Verified: both production constructions (`WorkerValidationService.java:365` and `:2093`) call the one-argument `new WorldMutationApplier(world)`, which installs `ChunkEditability.ALL_EDITABLE`, so **every chunk is editable in production**. Two further facts found 2026-07-30 say why the fix is not "pass the adapter". (1) **`ChunkLockMap` has no producer.** The only production `download` call is `WorldArchiveService:1255`, which passes `null` for the lock map; nothing anywhere calls `track`, so the map is empty and `isChunkEditable` answers `false` for every region — installing the adapter as it stands would make the whole world unbuildable, not fail-safe. (2) **There is no region-piece fetch at all.** Regions are *seeded* (`seedRegion`) and their manifests travel on `WorldManifestAnswer`, but no code downloads a region's pieces and applies them, so no production path exists in which a region's content arrives piecewise into a world a player is editing — the exact situation the guard exists for. Building that lane also needs the chunk→piece index, which today lives only in `RegionSnapshotSplitter.Layout` (derived from the snapshot the fetcher does not have) and does not travel with the manifest. So the honest sequence is: build the fetch lane, decide how the index travels (a coarser "the whole region is locked until complete" needs no wire change), and install the adapter with it — [network 4](Task.4.md) scope, recorded on L-33 |
| ~~`distribution.JoinAttemptThrottle`~~ | peer | **WIRED 2026-07-30 — security** | Zero non-test references, and an unreached throttle is an unthrottled join path. The live-join gate is a world password's only online guessing oracle, and its challenge is single-use *per connection* — so a client that reconnected got an unlimited supply of fresh nonces and the KDF's offline cost bought nothing. `HostJoinGate` now counts failures per (world, joiner), refuses a locked-out joiner **before** issuing a challenge (`JoinPasswordTask`) and returns `Verdict.THROTTLED` at `verify` so a caller that skips the pre-check still cannot grind. The key is the joiner's address, not the connection — keying on the connection would reset the counter on exactly the action being throttled (`JoinerIdentity`, `JoinerIdentityTest`). `JoinGateThrottleIsWiredTest` (7): 4 verified failing with the failure record removed |
| ~~`protocol.wire.MessageType.permits`~~ | transport | **WIRED 2026-07-30 — security** | Not in `structureReport` §2.2 (the class is reachable), which is why it survived the first sweep — but a grep for the *method* found one non-test caller in the repository, `MessageRouter`, which has no production caller either. So the NDR2 authorisation table held in its own tests and nowhere on a running node, and the state its comment describes as the defect it removed ("every row here was accepted from any connected socket") was still the state. `PeerRuntime.dispatch` now applies it once before any handler runs. Only six kinds carry `TRANSPORT_SENDER_EQUALS` and each is a self-statement never relayed for another peer, so a refusal cannot drop legitimate traffic; an unauthenticated carrier and a kind with no descriptor both still dispatch. `SenderAuthorisationIsEnforcedTest` (2, both verified failing without the gate) — a forged `PeerGoodbye` could evict any member and a forged `PeerJoin` could enrol a node that never joined |
| ~~`distribution.WorldArchive.Prepared`~~ | peer | **DELETED 2026-07-30** | The only entry in `structureReport` §2.1: referenced by nothing at all, tests included. Its two collaborators (`manifestFor`, `Bytes.unsafeWrap`) are public and reachable, so nothing was lost with it |
| `protocol.wire.MessageRouter`, `protocol.wire.CorrelationTable` | transport | **Still open — delete is blocked on a test, not on the verdict** (2026-08-06) | Plan 11 round 2 tried to carry the delete half out and could not: `CrossVersionIT` drives the router as its NDR2 forward-compatibility harness (unknown kind → `Nack`, v1 body → `Malformed`) and `AndroidTypeSwitchGuardTest` reads `MessageRouter.class`'s code source to find the compiled main classes, so neither is a dedicated test that leaves with the class. `CorrelationTable` is only reachable from `MessageRouterTest`. Deleting the pair means rehoming two real assertions first, which is a test-suite change rather than a dead-code deletion. Verdict unchanged, sequencing corrected. Original verification: `MessageRouter` has zero non-test, non-self references. Request/response correlation is a stated NDR2 capability, so the frame's `correlationId` is written by every send and consumed by nothing. Of the router's three declared columns, **authorisation is now enforced without it** (2026-07-30, at `PeerRuntime.dispatch`) because that half was a security property and could not wait for a god-class split. What is still only in the router is per-kind `HandlerMode` (the worker still fans one application lane out to six services by hand, last-wins) and the `UnknownKind` → `Nack` answer, so an unsupported kind is still a silent drop the sender can only diagnose as a timeout. Wiring the rest is the `PeerRuntime` split in the sequencing list below |
| ~~`transport.rendezvous.HolePunchCoordinator`~~ | transport | **DELETED 2026-08-06** (Plan 11 round 2, issue #210) — decided: delete, on the row's own terms. It says to wire it only with a measurement showing relay load is the binding constraint; no such measurement exists, and until one does the honest statement is the one the package javadoc now makes. Original reasoning: | Verified: the only non-test references are its own file and a `package-info` javadoc. `TransportSelector.Path` has exactly two values, `DIRECT` and `RELAYED` — there is no punched path for the selector to choose, so NAT traversal never runs and two peers that cannot reach each other directly are **always** relayed. That does not break envelope A-4: the bulk lane still reaches peers, through a relay. What it breaks is the cost model, because every such transfer is paid for by a third party's bandwidth, and the relay-scoring lane (`RendezvousDirectory`, service scoring) is what carries that cost today. Wire it only with a measurement that shows relay load is the binding constraint; until then the honest statement is "Nodera relays where it cannot dial", which is what `../network/Task.5.md` should say |
| `protocol.wire.MessageType.expectsResponse` | transport | Rides `MessageRouter` | The third of the router's declared columns. Authorisation is enforced (2026-07-30) and the `UnknownKind` answer is now sent by `PeerRuntime` directly; correlation is what is left, and it needs `CorrelationTable`, so both move together |

### The other two-thirds of the report — engine and storage, swept 2026-07-30

The inventory above was scoped to the four modules this category owns, so thirteen of the thirty
entries in `structureReport` §2.2 had never been read by anyone. They are listed here because the
register they belong to does not exist, and leaving them unlisted is how the first sweep missed the
handshake. **Not this category's to decide** — each needs its owning agent — but each was
grep-verified, and the pattern in them is a single one.

| Class | Module | What the grep found | Reading |
|---|---|---|---|
| ~~`coordinator.RegionAllocator`, `ProposalManager`, `HeartbeatMonitor`, `ReliabilityScorer`, `ServerVerifier`, `RendezvousPlacementPolicy`, `NodeRegistry`, `committee.CommitteeSession`, `committee.SpotCheckAuditor`, `shadow.ShadowCoordinator`~~ | engine | **DELETED 2026-08-06** (Plan 11 round 2, issue #210) — the row's own verdict, carried out, at cluster granularity: 30 production files and 21 dedicated test files, taking `ClientProposal`, `RegisteredNode`, `Reliability{Config,Factors}`, `CommitResult`, `consensus.{EquivocationDetector,EquivocationRecord,SpotCheckPolicy}`, the whole shadow worker/replica/divergence half and `core/region/RegionPlacementPolicy` with them. Only `shadow/SnapshotDeltaApplier` and `InterferenceProbe` are reached from production and they stay. Original reasoning: | **One finding, not ten: this is the retired central-coordinator design.** Task 30 removed the dedicated server, and ownership is now decided by `ViewOwnershipPlanner` from each player's render-distance disc — whose own javadoc says it is "a substitute for the server-side `RegionAllocator` in the decentralized model". So a whole server-authoritative layer is still compiled and shipped inside every jar. Deleting it is a category-sized decision for the engine agent, and it is the largest single block of dead weight the report measures |
| `coordinator.interference.AsyncActionGate` | engine | zero production references; `MutationGuard` and the mod's `BlockWriteGuard` only **name** it, in an exception message | Sharper than it looks: engine **L-25** retired on the pair "a documented rejection *and* a legal API". The rejection is genuinely wired (`LevelChunkMixin` → `BlockWriteGuard`), so the safety half holds — but the escape hatch a rejected mod is told to use, `AsyncActionGate.submit`, has no caller and nothing drains it. A third-party mod that follows the error message finds a door with nothing behind it. Not a safety regression; a completeness one, and the engine agent should decide whether L-25's second clause survives |
| `coordinator.EntityDelegabilityRules`, `coordinator.entity.JointTransferApprover` | engine | zero production references | `EntityDelegabilityRules` is the other half of the `RegionDelegabilityGate` row above — **both** implementations of delegability are unwired, so whatever refuses a region live is neither of them |
| ~~`simulation.border.ContraptionMigrator`~~ (+ `.Groups`) | engine | **DELETED 2026-08-06** (Plan 11 round 2, issue #210) | Cross-border contraption migration is a stated engine capability with no caller — and the `GroupMigration` wire message it would have sent has no sender either, so the lane was never built, only its decision core. The tag stays frozen in `WireRegistry`; `BorderSignal`'s javadoc now says the lane is unbuilt instead of naming a class that decides nothing |
| `storage.event.EventSourcedWorldStore` | storage | zero production references | Production is `RocksWorldStore` everywhere (`LiveEntityLaneSession`). The event-sourced tier ships, is tested, and stores nothing. Note it is *also* one half of the "route both stores through `EventChainGuard`" row in the duplication table above — that refactor would be deduplicating against a store nobody runs |
| `storage.client.BoundedClientWorldStore` | storage | zero production references; `StorageQuotaManager`'s mention is javadoc | The quota manager it was written to consult is reached by `FsContentStore` instead |

Read as one thing, the thirteen say: **the report's peer/transport third was the part anyone had ever
audited.** Two of the three registers that leaned on unwired code (network L-30, engine L-20) were
found by reading it; engine L-25's second clause is a third, found only because this sweep finally
looked at the other two-thirds.

Two rules for whoever takes this on. First, **a row is not dead because the tool says so** — the
report excludes anything whose caller could be outside our bytecode, but a call site added in the
same pass that reads this table will not be in it either; re-run `:peer:structureReport` rather than
trusting this list's date. Second, **wiring is not free**: every row above becomes a live code path
with live failure modes, so a row wired without a test that would fail if the call site were removed
has simply moved the defect (`CompanionSessionBindingIsCalledTest` is the cheap shape of that test).

## Completed

| Refactor | Evidence | Completed |
|---|---|---|
| The committee-lane IT fixture — nine `peer/.../peer/validation/*IT.java` each declared their own `Worker` record, their own fourteen-line factory, and their own copy of `uniformColumn`/`fullUniformSnapshot`/`signedPlace` | `dev.nodera.testkit.peer.{PeerTestHarness,ValidationNode,RegionFixtures}`; `scripts/test-totals.sh --java` 2,423 passed · 0 failed · 12 skipped, unchanged. Plan 11 phase 3, issue #212 | 2026-08-05 |
| `nodera-codec/tests` — `fixtures.rs` and `mutation.rs` each carried their own `fixtures_dir` / `fixture_files` / name-of-file lookup | `library/rust/nodera-codec/tests/common/mod.rs`; `cargo test -p nodera-codec` 79 passed. Routing of a tag to its codec family stays per-file on purpose — the two route differently | 2026-08-05 |

**A defect the second row uncovered.** `mutation.rs` read its wire tag as
`u16::from_be_bytes([golden[0], golden[1]])` — the first two bytes of the `NDR2` magic, not a tag —
so `round_trip_as` fell through to `DiscoveryMessage` for every fixture, and the rendezvous, service
and consensus frames were decode-failed and skipped by the `if let Ok(...)`. The mutation invariant
had therefore only ever been asserted on the discovery family. Putting `tag_of` beside it in
`common/` made the disagreement with `fixtures.rs` visible; the correct read passes.

**A blind spot worth recording.** Neither this register nor `../peer/REFACTORING.md` had a row for
the nine validation ITs, though they duplicated 30–80 lines apiece. Both were built from one `jscpd`
run, and jscpd's clone threshold does not catch a block that has been retyped rather than pasted —
the nine factories differ in identifier names and argument order while being the same code. A future
sweep should not read the absence of a row as evidence of no duplication.

## Sequencing

Ordered by ratio of risk removed to blast radius. Each item is one branch and one PR.

1. **Extract `IntegerEmaMeter` from `TickSkewMeter` / `TpsMeter`.** Highest duplication outside
   dead code, both classes are pure functions with injected time (the determinism rule already
   enforces this), and no frozen contract is involved. The safest place to land the first shared
   base and prove the pattern.
2. **Extract `DurableAppendJournal` from the two `Durable*Journal` siblings.** Same shape, same
   risk profile; both are local-only durability (the coordinator-state precedent), and the
   extraction is mechanical.
3. **Extract a `SealedRecord` helper for the signed canonical records** (`CertifiedWorldGenesis`,
   `WorldAdminProof`, `PeerCandidate`, `ServiceDirectoryEntry`). Four callers of one pattern;
   touches storage and transport but only behind each record's own `encode`, so the canonical
   bytes (the frozen contract) are unchanged and the fixture gate proves it.
4. **Remove the dead-tag runtime classes** (`EchoTest` 17, `RelayEnvelope` 18, handshake 1–4,
   `InventoryAdvertisement` 29, `ArchiveReplicaAssignment`/`Ack` 30/31). They are the dominant
   source of cross-file duplication and several are the #1 partner for unrelated records. Tag
   numbers stay frozen in `WireRegistry`; only the runtime classes and their tests go. Coordinate
   with the tracker/rendezvous agents because the Rust mirror still lists the tags.
5. **Derive `MessageCodec` and `InfrastructureCodec` dispatch from `WireRegistry`.** The largest
   god classes and the highest blast radius: both touch the frozen wire. Land it last, behind the
   existing fixture-regeneration and tag-mirror gates, and only after items 1–4 have reduced the
   noise those gates have to reason about.
