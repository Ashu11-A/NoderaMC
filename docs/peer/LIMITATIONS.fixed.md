# Worker — Retired Limitations

<!-- AI-AGENT-INSTRUCTION: Rows arrive here from LIMITATIONS.md when their EXIT TEST is green, WITH
      the evidence that retired them. Never delete a row or soften an evidence cell. If a retired
      capability regresses, open a bug issue and add a NEW row to LIMITATIONS.md. -->

**Category:** worker · **Last audit:** 2026-07-29 · Retired rows: **9**

| ID | Limitation | Retirement evidence | Owner | Retired |
|---|---|---|---|---|
| W-DUP-3 | **No launched worker process had ever used non-POSIX state storage.** Both secret-bearing writers pick their creation mode from the destination's `FileStore` and fail closed when advertised POSIX permissions are rejected, and `HeadlessPeerMainStateTest` drove the production `openLocalState` seam on zipfs — but zipfs is an in-process provider that `NODERA_STATE_DIR` cannot name, so the exit clause (a *launched* distribution) stayed open. Until it was run, "the worker boots from a FAT-formatted drive" was a claim about a code path, not an observation | The exit clause, verbatim, against the built distribution (`java/worker/build/install/nodera-headless/bin/nodera-headless`) on a real FAT32 loop mount — `Files.getFileStore(...).supportsFileAttributeView("posix") == false`, asserted before the run. Boot 1 minted an identity there; `NODERA-JOIN` wrote a registry row (`worlds.dat`, 79 bytes, on the mount); the process was killed and re-launched from the same paths; boot 2 answered `NODERA-IDENTITY` with the **same** node id (`5e3355be-…`) and `NODERA-WORLDS` with the row written before the restart. A second pass drove `NODERA-WORLDID`, so the third writer — a world's private key — was exercised too and landed as `<worldId>.worldkey` on the same mount. Neither log carried a POSIX or permission failure. Fix (landed 2026-07-28): `storage.io.AtomicFileWriter.writeOwnerOnly`, shared by `LocalFiles` and `PersistentIdentityStore`. **What this evidence is not:** an automated regression test — the exit needed a mounted filesystem and root to mount it, so it is a recorded manual run, and `AtomicFileWriterTest` (3) plus `HeadlessPeerMainStateTest` remain the parts CI can keep honest | [8](Task.8.md) | 2026-07-29 |
| W-DUP-1 | **A registry row was never reconciled against the world still being servable, so a stale entry was re-announced on every worker start, forever.** `restoreFromRegistry` reloads every `worlds.dat` row as hosted whether or not this node still holds the bytes, and nothing downstream re-checked. A save deleted outside the worker, or an archive directory wiped, left a row that told trackers and rendezvous services "join me for this world" for the rest of the node's life, and every joiner sent there got silence | `WorldHostingPersistenceTest#anUnservableWorldIsSuppressedWithinOneCycle` drives the exact exit test: a hosted world is announced, its servability is withdrawn, and **one** `reconcile()` pass later the world is suppressed from the announce set — no HEARTBEAT, no rendezvous refresh — while its row is retained both in the live set and in the `WorldRegistryStore` a restart would read; making it servable again resumes the announce under the **same** identity, with no second row and no re-derivation. `#aLiveWorldIsNeverSuppressed` pins the other direction: a world with a live `mcRoute` is servable by definition, so a lagging archive stream can never silence a world people are playing in. Fix: `WorldHostingService.bindServability` + the guard in `refreshAll`, bound in production to `WorldArchiveService.newestManifest(...).isPresent()` at `HeadlessPeerMain.java:180`. Suppression, not deletion, is deliberate — dropping the row would mint a second identity for the save when the content came back, which is the duplicate bug this bundle exists to end | [8](Task.8.md) | 2026-07-28 |
| W-DUP-2 | **`stop` removed the in-memory world that held the ownership record, so a later `seed` recreated it unowned** — a world this node authored, and still held the private key for, read as merely supported. The `.worldkey` survived on disk the whole time; nothing consulted it | `WorldHostingPersistenceTest#ownershipSurvivesStopAndReSeed` is the exit test verbatim: a world is hosted with its key on disk, `stop` drops both the live world and the registry row (the key is asserted still present), and the re-`seed` reads **owned** again — in the live world and in the `worlds.dat` a restart would read. `#aForeignWorldIsNotReAdministered` pins the boundary that makes this safe: a world this node holds no key for is never re-administered, so the repair cannot invent authority. Fix: `WorldHostingService.rebindOwnershipFromKeyStore`, called from `host`, `seed` and `restoreFromRegistry`, wired to the real `WorldKeyStore` at `HeadlessPeerMain.java:178-179`. The key's presence **is** the claim — keys are minted locally for a world this node authored and never accepted from the network — so the re-minted `WorldOwnership` carries a fresh `createdAtEpoch` and the **pinned** world id, never a re-derived one | [8](Task.8.md) | 2026-07-28 |
| W-DUP-4 | **Registries that already held duplicate rows for one save were not repaired**; the first pass of Task 8 only stopped them growing. A save re-shared after a genesis re-certification, or an identity file that could not be written, had minted a second id, and both ids stayed in `worlds.dat` forever | `WorldRegistryMergeToolTest` (9) covers `WorldRegistryMergeTool`, the one-shot repair. `#survivorIsChosenByTheOnDiskPin` is the exit test as written, driven entirely from disk: real signed `nodera-world.dat` pins discovered under a saves directory, real `.worldkey` files discovered under a world-keys directory, and the merged registry left with **one row per save** whose surviving id is the persisted pin. `#leavesOneRowPerSaveSurvivorChosenByThePin` and `#exactDuplicatesCollapse` cover the collapse rules; `#aSupportedWorldIsKeptEvenWhenNotPinned` proves the tool has no authority over a world this node did not create; `#ambiguousPinnedIdIsQuarantined` and `#conflictingOwnershipIsQuarantined` prove it quarantines rather than guesses when two saves pin one id or one id carries two administrators; `#dryRunWritesNothing`, `#aCleanRegistryIsLeftAlone` and `#anUndecodableRegistryAborts` prove it is byte-safe — dry-run writes nothing, a clean registry is untouched and unbacked-up, and a registry it cannot decode aborts instead of being overwritten. A real run copies the original to a timestamped `.bak` first, and orphaned `.worldkey` files are never deleted, so a wrong call is one rename from undone. **What this evidence is not:** an automatic repair. The tool is run by hand against a stopped worker (`WorldRegistryMergeTool#main`), deliberately — a repair that mutates state on every boot runs unattended against a file the worker is also writing | [8](Task.8.md) | 2026-07-28 |
| W-REPL-2 | **Supersede-eviction destroyed the only servable copy of a plaintext world.** Learning that a newer archive version existed evicted every older one — including the copy this node was serving — in exchange for a manifest it had never held. With the host's game closed, no peer held that newer version either, so every peer offered each other a version none of them had and `ContentTransferService.serve` answered each request with silence. Whole swarms reached `0/73 pieces` | `SupersededManifestEvictionTest` (3, encrypted fixtures matching the rule's stated intent) **and** `ArchiveFetchOverSocketsIT#onlyHeldVersionsAreOffered` (over real `SocketPeerTransport` on loopback) both green in the headless gate; the 2026-07-27 PROGRESS note records the fix landing with "1,970 Java tests green". Fix: eviction is scoped to **encrypted** versions (`supersedeOlderVersions` predicate `PieceManifest::encrypted`), because a password rotation is the only thing it can revoke and a plaintext archive has none; plaintext versions are bounded by the retention window (`trimToRetention`) instead. `WorldArchiveService.java:610-618`. The L-55 fixtures had been plaintext while documenting themselves as the re-key case — that mismatch is how the rule came to be applied where it protects nothing | [8](Task.8.md) | 2026-07-28 |
| W-REPL-3 | **A peer offered manifests for content it held nothing of.** `answerManifestQuery` returned every version this node knew about, so a peer that had merely *heard* of a version advertised it as fetchable; the asker picked the highest, requested its pieces, and got silence. The silent drop had no log, no reply and no metric anywhere — which is why it took four live rounds to find | `ArchiveFetchOverSocketsIT` (3 `@Test` over real TCP on loopback, incl. `#onlyHeldVersionsAreOffered` and `#aSilentBystanderDoesNotStallTheFetch`) green in the headless gate; the 2026-07-27 PROGRESS note records the fix landing with "1,970 Java tests green". Fix: only versions this node holds at least one piece of are offered (`WorldArchiveService.answerManifestQuery`, `WorldArchiveService.java:916-950`), paired with a `requestsForUnknownContent` counter so a request for an unknown root is no longer dropped in silence on the serving side either | [8](Task.8.md) | 2026-07-28 |
| L-41 | The always-on out-of-game process could not keep a world whole on its own: the periodic tracker announce and rendezvous registration ran, but nothing proved they described what this node holds *now*, and the validated lane's **region pieces** were never seeded at all — `RegionSnapshotSplitter` had no caller outside tests, so a joiner could fetch a world's save from the worker and never a certified region of it | `SeedRegionVerbIT` drives the whole clause over a real loopback control socket: a world is hosted, its save is seeded, and three committed region snapshots are pushed over `NODERA-SEED-REGION` on **one connection each, every one closed on the way out** — the caller is transient by construction, exactly like the game process it models. With nothing connected, both lanes are still held and both still ride one announce (`holdingsFor` returns the save's manifest and the region's), and a file that is not a region snapshot is refused rather than advertised as content no fetcher could use. `WorkerHeartbeatHoldingsTest` pins the timer half where it can actually rot: the heartbeat re-reads holdings on every announce (holdings captured at HOST would leave a region seeded later invisible for the whole session), an unreachable tracker never stops it, and the cadence keeps its floor. `RegionPieceSeedingTest` (7) and `CommittedRegionSeederTest` (6) pin the seeding rules — a separate ladder per lane so a region at version 9 cannot supersede the save at version 8, an idempotent re-seed, a bounded per-region window, a deterministic capped announce, and a refusal to attribute a commit to a world when this node hosts several. `RegionSeedSpoolTest` (7) covers the mod half: never on the commit thread, throttled per region, bounded backlog that drops rather than grows, and every failure contained. **What this evidence is not:** a run with a real Minecraft client. The pushing side is modelled by its control-channel behaviour, which is exactly what the mod is over that channel | [3](Task.3.md) | 2026-07-26 |
| L-77 | The worker could not record a consent decision, so the app's first-run question had nowhere to be stored and no node measured itself. A player who *wanted* to help improve NoderaMC had no way to, and the project had no population-level evidence about anything | `TelemetryVerbIT` round-trips a decision over the real control socket and reads it back from a **new emitter over the same directory** (the record lives with the worker, not with whatever process asked). `WorkerTelemetryServiceTest` pins that revocation clears the queue and deletes the install id. The isolation half is `scripts/e2e-telemetry.sh` **T6**: the collector is killed mid-run and the worker's whole `NODERA-STATE` answer is compared field by field against the pre-kill one — everything except the telemetry block and the clock is identical, and `PROBE`/`IDENTITY` keep answering. T1 proves the other direction: a worker nobody asked lets two collection windows pass and writes nothing at all | [5](Task.5.md) | 2026-07-25 |
| L-48 | The always-on node could not **validate** regions: a companion-only peer was seeder, relay, and router capable only, and the entire validation stack was runtime-unreferenced outside the mod — the `simulationmsg` wire family had no live consumer at all | `WorkerValidationService` runs committee re-execution **out of game** and participates in quorum over the same `PeerTransport` the membership session rides. `WorkerQuorumValidationIT` proves the exit: three **companion-only** worker nodes (no Minecraft process) form a committee over registered peer runtimes — the primary proposes, validators re-execute with **the** engine and vote over the wire, the 2-of-3 quorum commits, every worker converges on the byte-identical root **matching the reference engine**, and each persists the co-signed certificate in its own store, so certified state flows peer to peer. Primary loss promotes a validator under epoch+1 and the surviving 2-member committee keeps committing. The formerly orphaned fallback lane executes an unassigned-region action through the server lane and the soak ratio holds. `HeadlessPeerMain` constructs the lane and `STATE` reports live validation counters | [4](Task.4.md) | 2026-07-21 |
