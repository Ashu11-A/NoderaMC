{
  "note": "Ratchet for the structural code report (`./gradlew :peer:structureReport`). Every number below is production code that ships and does nothing: classes nothing references, methods only tests call, methods no entry point can reach, methods too big for the JIT, and severe in-loop cost findings. They may only go DOWN — raising a limit to make a build green defeats the point of measuring, so a rise fails the task. The task prints the tightened set whenever the tree improves; paste it here in the same commit.",
  "measured": "2026-08-06 on the MERGED round-3 tree (Plan 11 round 3: the EventBus and traversal fixes, the dead-lane wiring, the delegability decision, the named-file levers, and the error-rate checks), re-measured by `./gradlew :peer:structureReport` on the merge commit rather than carried over from any one branch. Against the round-2 stamp: never_referenced_methods 4 -> 2, test_only_methods 318 -> 314, unreachable_methods 117 -> 116; the other four counters did not move. Re-measuring rather than merging matters here for a reason round 3 made concrete: two of the five branches reported different values for never_referenced_methods (4 and 2) because each was cut before the other merged, and each was correct about the tree it described. A ratchet file describes exactly one tree, and neither branch's tree survives the merge. Tightened again after the validation pass: test_only_classes 12 -> 11, test_only_methods 314 -> 312, unreachable_methods 116 -> 115, from deleting SessionParams -- a class this PR itself orphaned by deleting both of its production callers, whose only remaining consumer was one test. never_referenced_methods at 2 has no headroom; both are documented in STRUCTURE.md and each is a filed defect kept fixable-by-wiring rather than deleted.",
  "limits": {
    "dead_classes": 0,
    "test_only_classes": 11,
    "never_referenced_methods": 2,
    "test_only_methods": 312,
    "unreachable_methods": 115,
    "huge_methods": 0,
    "severe_cost_findings": 3
  }
}
