# Generated at build time from the root VERSION file (see java/core/build.gradle.kts).
# The placeholder below is expanded by processResources; a jar containing the literal
# placeholder means the resource was read from the source tree, not from a build.
version=${noderaVersion}
