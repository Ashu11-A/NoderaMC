/**
 * Nodera core — Minecraft-free domain vocabulary: identity, region, action, state,
 * event, consensus-certificates, and the canonical encoding / hashing / signing services.
 *
 * <p>Layering rule (Task 0 §4.1): {@code core} depends on nothing but the JDK.
 */
package dev.nodera.core;
