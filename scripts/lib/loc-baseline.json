{
  "note": "Ratchet for `scripts/loc-metrics.py`. Every number below is lines of git-tracked source, classified by the lexer in `scripts/lib/loc_classify.py`. They may only go DOWN \u2014 raising a limit to make a build green defeats the point of measuring, so a rise fails the gate. When a rise is deliberate, run `scripts/loc-metrics.py --baseline` and commit the new file in the SAME commit that grew the tree. Generated files (`@generated` / `DO NOT EDIT` in the header) are measured but excluded from these limits, because appending a wire kind must never fail a size gate. The `*.comment` numbers are measured and diffed but NOT gated: a gate that makes deleting documentation the cheapest way to go green is pointed at the wrong thing. What these limits do NOT cover, stated so the total has a denominator: the programme's own tooling, 11 `.py` files (3,434 code lines) and 25 `.sh` files (3,821), 7,255 code lines in all -- roughly 4.4% of the tree, and rising, because this tooling is inside its own blind spot. Counting them needs a `#`-comment lexer for two more languages, here-documents and docstrings included, and until that exists moving logic into a script still reads as reduction.",
  "measured": "2026-08-10 on the `test/full-live-matrix-#187` branch, by `scripts/loc-metrics.py --baseline`, on the COMBINED tree of four unmerged PRs: #257 (`chore/e2e-live-plan-jdk-#142`), #259 (`fix/harness-tells-the-truth-#188`), #268 (`fix/harness-ports-cannot-collide-with-a-real-install-#266`) and #260 (`feat/cross-folia-region-commit-#179`). This branch adds no production or test code of its own; it exists to EXECUTE the live matrix against the tree those four produce.\n\nThis stamp is therefore not a growth claim, it is the arithmetic of a merge. Each of the four branches stamped this file against `main`, so each stamp is blind to the other three, and every one of them is too small for the union: checking the merged tree against the last-merged stamp reported `java.main.code` +2 and `java.test.code` +1043. Neither `--ours` nor `--theirs` is a correct resolution of this file \u2014 both silently loosen or falsely tighten the gate \u2014 so it is re-measured here, which is the only resolution that means anything.\n\nTRAP, kept from the #142 stamp because it has now cost two agents a red CI job: this gate reads `git ls-files`, not the working tree, so stamping before `git add` omits every new file and the local `--check` then passes against the same blind view. The order is always `git add`, then `--baseline`, then `--check`. The tree was fully committed before this measurement.\n\nNOTE FOR THE INTEGRATOR: this file cannot be merged. If the four PRs land in a different order, or any of them changes before merge, re-measure again on whatever tree results.",
  "limits": {
    "java.main.code": 56846,
    "java.main.comment": 33930,
    "java.test.code": 56619,
    "java.test.comment": 13075,
    "kotlin.main.code": 4015,
    "kotlin.main.comment": 1043,
    "kotlin.test.code": 21,
    "kotlin.test.comment": 34,
    "rust.main.code": 19698,
    "rust.main.comment": 7799,
    "rust.test.code": 12264,
    "rust.test.comment": 1134,
    "ts.main.code": 12588,
    "ts.main.comment": 4260,
    "ts.test.code": 3544,
    "ts.test.comment": 1677
  }
}
