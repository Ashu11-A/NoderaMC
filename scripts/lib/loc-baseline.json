{
  "note": "Ratchet for `scripts/loc-metrics.py`. Every number below is lines of git-tracked source, classified by the lexer in `scripts/lib/loc_classify.py`. They may only go DOWN \u2014 raising a limit to make a build green defeats the point of measuring, so a rise fails the gate. When a rise is deliberate, run `scripts/loc-metrics.py --baseline` and commit the new file in the SAME commit that grew the tree. Generated files (`@generated` / `DO NOT EDIT` in the header) are measured but excluded from these limits, because appending a wire kind must never fail a size gate. The `*.comment` numbers are measured and diffed but NOT gated: a gate that makes deleting documentation the cheapest way to go green is pointed at the wrong thing.",
  "measured": "2026-08-06 on the MERGED round-2 tree. Total code 167,711 -> 158,310 (-9,401, -5.61%). rust.test +172 and ts.test +16 are the round's new regression tests -- seven Rust defect fixes, the frame-size bound asserted for all three services, the UDP amplification cap, and the wire-mutation corpus assertions -- stamped up deliberately because a size gate that charges for a regression test is pointed at the wrong thing. Re-measured on the merge commit, not merged from any branch: five branches each describe a tree that stops existing at the merge, and taking any one branch's stamp silently loosens or tightens the gate.",
  "limits": {
    "java.main.code": 56385,
    "java.main.comment": 33287,
    "java.test.code": 54516,
    "java.test.comment": 11558,
    "rust.main.code": 19571,
    "rust.main.comment": 7706,
    "rust.test.code": 12017,
    "rust.test.comment": 1063,
    "ts.main.code": 12588,
    "ts.main.comment": 4260,
    "ts.test.code": 3233,
    "ts.test.comment": 1369
  }
}
