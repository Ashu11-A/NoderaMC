{
  "note": "Ratchet for `scripts/loc-metrics.py`. Every number below is lines of git-tracked source, classified by the lexer in `scripts/lib/loc_classify.py`. They may only go DOWN — raising a limit to make a build green defeats the point of measuring, so a rise fails the gate. When a rise is deliberate, run `scripts/loc-metrics.py --baseline` and commit the new file in the SAME commit that grew the tree. Generated files (`@generated` / `DO NOT EDIT` in the header) are measured but excluded from these limits, because appending a wire kind must never fail a size gate. The `*.comment` numbers are measured and diffed but NOT gated: a gate that makes deleting documentation the cheapest way to go green is pointed at the wrong thing. What these limits do NOT cover, stated so the total has a denominator: the programme's own tooling, 11 `.py` files (3,434 code lines) and 25 `.sh` files (3,821), 7,255 code lines in all -- roughly 4.4% of the tree, and rising, because this tooling is inside its own blind spot. Counting them needs a `#`-comment lexer for two more languages, here-documents and docstrings included, and until that exists moving logic into a script still reads as reduction.",
  "measured": "2026-08-10 at ee6d61f on the `feat/endpoint-tick-lanes-#177` tree (issues #177 + #180), by `scripts/loc-metrics.py --baseline` run AFTER `git add -A` — the baseline measures `git ls-files`, so stamping before staging silently omits every new file and CI then reports a larger number than the local check did.\n\nThe rise is all new work rather than churn: `ProjectionPinner` + `BukkitProjections` + `ProjectionListener` in `:paper-plugin` (server task 5 deliverable 6, L-69) with their wiring in `NoderaEndpointPlugin`; the `endpoint` P6 and `folia` F5 live stages plus `ServerEndpointSupport.requireDelegatedRegion`; `ProjectionPinnerTest` (12) and `InterferenceThroughputTest` (4). The comment share is high because two of these files exist to record a derivation — the thresholds L-67's exit clause is stated in, and the two reasons the design as previously written could not have met L-69's — and the reasoning is the deliverable, not decoration.\n\nEvery bucket sits exactly on its measured value, because that is what `--baseline` writes; there is never any headroom, so a one-line fix and its test cost a stamp. That pressure is the known cost of an exact ratchet, and the answer to it is a reason recorded here, not a limit with slack in it. **This file describes exactly one tree**: whoever merges this branch with another must re-run `--baseline` on the combined tree in the merge commit, or the gate silently loosens.",
  "limits": {
    "java.main.code": 56759,
    "java.main.comment": 33892,
    "java.test.code": 55744,
    "java.test.comment": 12538,
    "kotlin.main.code": 4012,
    "kotlin.main.comment": 1030,
    "kotlin.test.code": 21,
    "kotlin.test.comment": 34,
    "rust.main.code": 19698,
    "rust.main.comment": 7799,
    "rust.test.code": 12264,
    "rust.test.comment": 1134,
    "ts.main.code": 12588,
    "ts.main.comment": 4260,
    "ts.test.code": 3544,
    "ts.test.comment": 1677
  }
}
