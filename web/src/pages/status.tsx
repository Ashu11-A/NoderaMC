export default function Page() {
  return (
    <>
      <h1>Where the build actually stands</h1>
      <p>Task counts, measured test totals and every open limitation, read out of the repository rather than typed here.</p>
    </>
  );
}
